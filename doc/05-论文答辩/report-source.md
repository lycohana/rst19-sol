# rst19 论文研究来源与证据账本

> 这是论文短稿的来源账本，不是正文。它把外部方法依据、项目实测事实和当前未知量分开，便于答辩时追溯，避免把文献中的条件性结论写成普适真值。

## 研究主张

| 主张 | 直接来源 | 在 rst19 中的对应证据 | 不能推出 |
| --- | --- | --- | --- |
| 局部峰是候选响应，不等于物理源数 | [DAOPHOT](https://articles.adsabs.harvard.org/pdf/1987PASP...99..191S)、[StarFinder](https://arxiv.org/abs/astro-ph/0004101)、[StarNet](https://www.jmlr.org/papers/v24/21-0169.html) | 单源/双源 PSF、候选/质量分层 | 两个峰一定是两颗星 |
| 可解析性由 PSF、分离、亮度比和方向共同决定 | [Penoyre, 2026](https://doi.org/10.1093/rasti/rzaf062) | 5.4 px 等强与不等强双源注入 | 5.4 px 是普适分辨率阈值 |
| 完备率应由已知真值注入回收，而不是由峰数反推 | [ComEst](https://arxiv.org/abs/1605.02650) | 真实背景分层注入、成对一对一回收 | 注入召回率等于真实图像 precision |
| 高 SNR 不能排除结构性异常 | [L.A.Cosmic](https://arxiv.org/abs/astro-ph/0108003)、[Rhoads](https://arxiv.org/abs/astro-ph/0002041) | 线状、范围异常、尖峰类别的高 SNR 反例 | 亮点必然是宇宙线或必然是真星 |
| PSF 估计需考虑模板选择与空间变化 | [PSFEx](https://www.astromatic.net/wp-content/uploads/psfex_article.pdf)、[Wohlberg & Wozniak](https://arxiv.org/abs/2101.01268) | 20 个模板源、全局/局部 PSF 抽检 | 当前全局经验核已代表所有 detector 位置 |
| 近邻多峰的源数需要比较多个 PSF 模型，而不能由局部峰数决定 | [Kass & Raftery 1995](https://doi.org/10.1080/01621459.1995.10476572)、[DAOPHOT](https://articles.adsabs.harvard.edu/pdf/1987PASP...99..191S)、[StarFinder](https://arxiv.org/abs/astro-ph/0004101)、[Hedges et al. 2021](https://arxiv.org/abs/2106.08411) | 固定位置 `K=1/2/3` 的非负 Gaussian PSF、BIC、分量 SNR 和掩膜敏感性审计 | `ΔBIC` 越大就等于物理上已确认的多颗恒星；自由位置/空间变 PSF 仍未完成 |
| 固定中心误差与自由度过拟合必须用有界位置敏感性分开审计 | [DAOPHOT](https://articles.adsabs.harvard.org/pdf/1987PASP...99..191S)、[StarFinder](https://arxiv.org/abs/astro-ph/0004101)、[Penoyre 2026](https://doi.org/10.1093/rasti/rzaf062) | `r=0.25/0.5/0.75/1.0/1.25 px` 自由位置扫描、位置触边和优化收敛字段 | 某个搜索半径下通过就代表半径无关的源身份；有界 Gaussian 仍不是完整联合似然 |
| 主办方路线也采用亮星估计 PSF、增强暗目标和时序跟踪 | [StarImage_SimuIden](https://github.com/Aoyeww/StarImage_SimuIden) 及其页面列出的 [IEEE Sensors Journal 论文](https://doi.org/10.1109/JSEN.2024.3350089)、[IEEE TIM 论文](https://doi.org/10.1109/TIM.2025.3573015) | rst19 的宽筛—细筛、经验 PSF、序列分支 | 主办方实现输出就是本数据真值 |
| 全幅重复高位码与局部异常负值应先作为数据有效性模式审计 | [HST/ACS detector considerations](https://hst-docs.stsci.edu/acsdhb/chapter-4-acs-data-processing-considerations/4-3-dark-current-hot-pixels-and-cosmic-rays)、[2MASS reliability](https://www.ipac.caltech.edu/2mass/releases/allsky/doc/seca5_1.html) | F01 `3990--3993` 全幅频次、局部负值共现、`CODE_PATTERN` 质量门 | 这些统计模式已经等价于坏像素、ADC 饱和或普通白噪声真值 |
| 类别跨帧持久性必须同时看候选层和质量层 | [ComEst](https://arxiv.org/abs/1605.02650)、[Zackay et al.](https://arxiv.org/abs/1601.02655) | 最新 15 帧 `12/15` 邻域审计：范围异常 `4/44` 候选持久、`2/44` 质量邻域；紧凑质量 `25,056/26,483` 候选持久、`21,573` 质量邻域 | `12/15` 响应就是逐星真阳性或真实恒星完备率 |
| 多提议器交集只能作为复核优先级，不能当作真星投票 | [WAVDETECT](https://arxiv.org/abs/astro-ph/0108429)、[DAOPHOT](https://articles.adsabs.harvard.org/pdf/1987PASP...99..191S)、[StarFinder](https://arxiv.org/abs/astro-ph/0004101) | `crowded_blend` 的 `902/905` 为 DoG-only 且全部 `UNRESOLVED_BLEND`；`compact_quality` 的 `23,848/26,483` 为三提议器交集，另有 `286` 个无 Gaussian 候选通过质量层 | DoG-only 一定是假源；三者交集就是逐星概率 |
| 线状结构应使用拖尾/轨迹模型，不应混进圆形点源计数 | [Astreaks 2023](https://academic.oup.com/mnras/article/524/2/2651/7218582)、[GOTO pipeline](https://academic.oup.com/rasti/article/doi/10.1093/rasti/rzag042/8707248) | `LINE_ARTIFACT` 分流、连通域几何、15 帧轨迹关联；当前线状候选单独报告 | 单帧高 SNR 线就是已确认运动目标 |
| 复杂/拥挤形态需要显式去混叠 | [DRUID 2025](https://academic.oup.com/rasti/article/doi/10.1093/rasti/rzaf006/8043275)、[Hedges et al. 2021](https://arxiv.org/abs/2106.08411) | DoG-only 近邻分叉、单/双 PSF BIC、注入组级解析率 | 局部峰数直接等于物理源数 |

## 项目中已经测得的事实

1. 当前 GUI 默认首帧一次运行返回 `84,594` 个宽筛候选、`29,260` 个质量规则通过源；`29,263` 是近邻联合门控修补前的质量层基线，`29,271` 是只加入极端负码审计的中间口径，`29,153` 是 `CODE_PATTERN` 初版（仅“孔径内含重复码”即拒）的历史口径，这些数字都不是星表真值。最新同参源级重跑为 `tmp/source-quality-audit-code-pattern-current-v3/`；其中 `source_feature_method_summary.csv` 记录提议器来源交集。
- 当前 `hybrid + empirical PSF` 真实背景六条件分层注入复跑产物为 `tmp/stratified-real-background-code-pattern-current-n8/`；它保持严格新增分母与 8 px 邻源歧义分开，不能把特殊值域或线状邻域的结果解释成孤立星完备率。
- 同一背景口径的重复布局复核产物为 `tmp/stratified-real-background-code-pattern-current-t2-s4/`；与上一目录合并后每层每档为 16 个位置，拥挤层召回变化被记录为布局敏感性，不作为固定分辨率。
- 当前口径的 2/3 源组级 Gaussian/经验 PSF 对照产物为 `tmp/crowded-blend-audit-code-pattern-current-t2-g8-gaussian/` 与 `tmp/crowded-blend-audit-code-pattern-current-t1-g4-empirical/`；这些表将候选解析、质量解析、合并率分开，不能把候选峰数量当成物理源数。
2. 截图局部两个响应相距约 `5.39 px`；副响应有 `NEGATIVE_OVERFLOW`、固定 `-1` 邻域和重复正值结构，原始双 PSF `ΔBIC=-2.59`、次分量 SNR 约 `1.55`，经验 PSF 对照也未达到双源确认线。注册版 F07 只有在屏蔽范围码或精确 `-1` 后形式上越过工程线，原始分量 SNR 为 `4.511` 且该帧带 `MASKED|SATURATED`，因此只记为观测模型敏感性。
3. 同一真实背景的 Gaussian 双源控制显示，固定 `r=0.125` 时总峰值 `256/512/1024 ADU` 的成对质量解析率为 `0/0.25/1.0`；这说明弱副源先在质量层失去证据，再在宽筛层消失。
4. 经验 PSF 模板池为 `20` 个源，中位 FWHM `2.4966 px`、归一化核和 `3.6465`。在 detector FWHM `2 px` 的形状敏感性控制中，`512 ADU`、`r=0.25/0.125` 的成对质量解析率为 `0.25/0`；匹配尺度 `2.5 px` 探针仍为 `0.25/0`，同尺度 Gaussian 为 `1.0/0.5`，但两次布局随 detector FWHM 改变，不能当作严格逐点排名。
5. 真实首帧的 `-1` 固定坐标审计得到 `74` 个跨 15 帧稳定坐标、总精确 `-1` 观测 `2,185`；但格式说明没有把 `-1` 定义为坏像素或无效码，因此正文只称“固定无效码候选”。
6. 为控制 PSF 核和混杂，固定总离散积分超额 `1867 ADU`、同一布局、`2 trials × 8 对` 后，Gaussian/经验 PSF 在 `r=0.25` 的成对质量解析率为 `0.812/0.625`，在 `r=0.125` 为 `0.188/0.125`；候选层分别为 `1.000/0.938` 与 `0.688/0.812`。这支持“PSF 形状影响细筛，候选层和质量层可反向排序”，但不是最终 PSF 排名。
7. 在同一首帧把空间 PSF 诊断扩大到 `2×2` 网格、每类 `16` 个样本，并在近邻联合门控后的当前代码上重算：紧凑质量类全局/局部相关中位数 `0.862/0.837`、相对残差 `0.507/0.547`，局部有效 `11/16`；线状类 `0.412→0.405`、残差 `0.911→0.914`、有效 `16/16`；拥挤类 `0.156→0.172`、残差 `0.988→0.985`、有效 `11/16`。这支持“紧凑源没有稳定系统性空间改善，线状结构不是点源，拥挤问题主要是混合/去混叠”，但仍不是空间变 PSF 的最终实现。当前产物为 `tmp/source-feature-psf-grid2-per16-code-pattern-current-v4/source_feature_psf_spatial.csv`，旧 `gui-default` 目录为门控前历史对照。
8. 在同一真实背景注入 2 源/3 源拥挤组并扩展到每条件 `2 trials × 8` 组后，`3 px` 相邻组的候选合并率为 `0.75--1.0`、组级解析率为 `0`；`5.4/6 px` 候选组级解析率为 `1.0`，但质量级完整解析对每源信号和组源数仍敏感。两档总积分信号为 `512/1024 ADU`；2 源在 1024 ADU 的质量解析为 `15/16`、`14/16`，3 源为 `1/16`、`0/16`，因此仍只是机制证据。
9. 经验 PSF 的 4 组/条件控制中，`3 px` 组级解析仍为 `0`，`5.4 px` 候选组级解析为 `1.0`，而 `6 px` 候选组级解析在 2 源/3 源条件下出现 `0.75/0.25`；因此间距结果受 PSF 形状与每源信号影响，不能写成普适分辨率。
10. 对截图三个位置做固定位置 `K=1/2/3` 审计时，raw 原图仅 F07、F12 的 `K=2` BIC 比 `K=1` 更优，但新增分量 SNR 分别为 `4.38/4.00`，没有一帧同时达到 `ΔBIC≥10`、`SNR≥5`；`K=3` 没有越线。屏蔽范围码后只有 F07 形式上越线（`ΔBIC=19.98`、新增分量 SNR `5.14`），但该帧带 `MASKED|SATURATED`，因此属于模型敏感性，不是原始真值确认。

11. 有界自由位置 raw `K=2` 半径扫描在 `r=0.25/0.5/0.75/1.0/1.25 px` 下分别确认 `0/15、0/15、0/15、1/15、1/15`；F07 只有在允许约 `1 px` 移动后才达到当前局部确认线，窄半径时新增中心触碰边界。固定位置 `0/15` 与自由位置 `1/15` 的差异记录为模型自由度敏感性，不写成双星比例。
12. 第二组截图的首帧两个提案约为 `(2439,4021)` 与 `(2435,4022)`；峰坐标间距 `4.12 px`、测量质心间距约 `2.74 px`。在加入 `CODE_PATTERN` 前，两者的峰值/通量 SNR/FWHM/支持像素约为 `3992/204.61/2.59/9` 与 `569/115.51/2.44/8`，因此旧质量规则会把它们同时保留。
13. 第二组截图固定位置 raw `K=2` 的 15 帧 `ΔBIC` 全部为负（约 `-4.40` 到 `-2.98`），自由位置半径 `0.25/0.5/0.75/1.0/1.25 px` 的确认数全部为 `0/15`；同时屏蔽固定 `-1` 与正负范围异常的对照仍为 `0/15`（`ΔBIC` 约 `-4.40` 到 `-0.95`）。新增 `CODE_PATTERN` 后，F01 候选数仍为 `84,594`，质量数从门控前 `29,295` 变为 `29,263`；两个提案保留在候选审计层但不再进入质量层。
14. 最新 `CODE_PATTERN`（峰值落在重复码上才拒绝）和近邻联合门控版本的首帧范围异常候选为 `44`（`NEGATIVE_OVERFLOW 36`、`CODE_PATTERN 12`、`SATURATED 1` 的并集；初版“孔径内含重复码即拒”口径为 `159`）。按修正口径重跑后的 15 帧特征持久性为：范围异常 `44/4/2`，紧凑质量 `26,483/25,056/21,573`，弱源 `21,250/16,128/404`，尖峰 `23,824/17,521/87`，拥挤 `905/509/6`，线状 `96/22/16`（依次为首帧候选/候选持久/质量持久）。

15. 第二组截图的固定位置 Gaussian FWHM 扫描显示，raw `K=2` 的 `ΔBIC>0` 帧数随 FWHM `1.5/2.0/2.5/3.0/3.5 px` 为 `0/15、0/15、3/15、11/15、11/15`；最宽模型的 `ΔBIC≥10` 为 `7/15`，但副分量最大 SNR `4.54`，确认线仍为 `0/15`。这条证据用于防止把 PSF 失配造成的模型偏好写成第二颗星。
16. 第二组截图最新原始值域复核与修补后复算：主/副峰坐标 `(2439,4021)` / `(2435,4022)` 的整数峰间距 `4.12 px`，测量质心间距约 `2.74 px`，半径 `4 px` 孔径重叠 `18/49` 像素；首帧局部窗含 `−21691/−23156`、重复码 `3991/3992` 和局部最大 `13028`。主峰 `3992` 触发 `CODE_PATTERN` 而副峰 `569` 不触发，修补前近邻 Gaussian 门控只比较两个质量通过源，因此会留下一个普通质量副候选。本轮将带值域异常旗标的强邻峰纳入联合审计并用测量质心配对；正式首帧测试 `7` 组近邻、标记 `7` 个 `UNRESOLVED_BLEND`，质量数由 `29,263` 变为 `29,260`，截图副响应的 `ΔBIC=-4.09`、次分量 SNR `0.87`。候选仍保留，副框不升级为第二颗星。`min_distance=4/5/6` 的数量是修补前 NMS 基线 `84,594/29,263`、`80,117/28,374`、`73,118/27,101`；最新源级/时序/交叉产物分别位于 `tmp/source-quality-audit-code-pattern-current-v3/`、`tmp/sequence-feature-persistence-code-pattern-current-v3/` 和 `tmp/feature-cross-audit-code-pattern-current-v3/`；直接依据为 `doc/02-星图识别/检测器实验记录.md` 8.49、`doc/02-星图识别/亮点真实性与伪影判别研究.md` 10.32--10.35 及 `tmp/local-multipsf-fwhm-sweep-pair2-f2.0/local_multipsf_audit.csv`。
17. 最新 15 帧提议器来源复核显示，`crowded_blend` 的 `13,764` 条帧级候选记录中 `13,739` 条为 DoG-only（`99.82%`），但首帧锚点候选/质量 `12/15` 持久性只有 `56.2%/0.7%`；`compact_quality` 的 `396,006` 条帧级记录中 `90.10%` 三路共同支持，另有 `4,105` 条（`1.04%`）DoG-only 仍通过质量层。该结果支持“Gaussian+DoG 并集宽筛、按值域/PSF/形态/跨帧/星表或注入细筛”，但帧级来源计数不是去重恒星数。机器产物为 `tmp/sequence-feature-persistence-code-pattern-current-v3/sequence_feature_method_frame_summary.csv` 和 `sequence_feature_method_profile.csv`。

18. 首帧 `compact_quality + dog_only` 的 `286` 个候选又拆成 `61` 个近邻双 PSF 审计记录和 `225` 个无双 PSF 记录。前者最近 Gaussian 距离中位数 `4.243 px`、FWHM 中位数 `3.414 px`，`61/61` 达到 `ΔBIC≥10` 且分量 `SNR≥5`；后者最近 Gaussian 均 `≥5 px`、中位距离 `5.099 px`、FWHM 中位数 `3.574 px`。沿 15 帧配准坐标逐源复核，两组达到候选 `12/15` 的分别为 `24/61`、`160/225`，质量 `12/15` 的分别为 `12/61`、`102/225`。缺少双 PSF 字段表示没有进入默认近邻审计适用域，不代表拟合失败；因此分别写作“近邻双分量待去混叠”和“尺度/PSF 待验证”，不直接换算恒星数。依据为 `doc/02-星图识别/检测器实验记录.md` 8.53、`doc/02-星图识别/亮点真实性与伪影判别研究.md` 10.36，机器源表为 `tmp/source-quality-audit-code-pattern-current-v3/source_catalog.csv`，逐源时序表为 `tmp/sequence-feature-persistence-code-pattern-current-v4/sequence_feature_source_subgroup_persistence.csv`。
19. 新增诊断子组时序审计：对首帧非 `compact_quality` 类按固定优先级拆分互斥子组，并分别统计位置候选持久性与同子组持久性。15 帧结果为：边缘/掩膜 `9,590/11,971=80.1%` 对 `321/11,971=2.7%`，拥挤 `509/905=56.2%` 对 `196/905=21.7%`，尖峰/支持 `17,521/23,824=73.5%` 对 `7,289/23,824=30.6%`，弱源 `16,128/21,250=75.9%` 对 `2,723/21,250=12.8%`，线状 `22/96=22.9%` 对 `0/96`，范围异常 `4/44=9.1%` 对 `0/44`，形状异常 `20/21=95.2%` 对 `2/21=9.5%`。子组中部分掩膜为位置 `80.1%`、同机制 `0.1%`，纯边界为 `80.0%`、`52.5%`；尖峰+支持不足为 `100%`、`37.4%`。这说明“位置重复−同机制重复”的落差可作机制分流信号，但不是星表身份、真星概率或伪影率。依据为 `doc/02-星图识别/检测器实验记录.md` 8.54、`doc/02-星图识别/亮点真实性与伪影判别研究.md` 10.37，机器表为 `tmp/sequence-feature-persistence-diagnostic-subgroups-v5/sequence_feature_diagnostic_subgroup_persistence.csv`。

20. 未降噪原图分层抽样复核：每个非 `compact_quality` 诊断子组最多抽取 `12` 个首帧源，另取 `24` 个紧凑质量对照，逐源检查 15 帧原始 FITS 的 `r<=4` 孔径、`6<=r<=8` 背景环、峰值/通量 SNR、核心能量占比、FWHM 和 3×3 支持。样本中位数为：紧凑对照 `peak/flux/core/FWHM/support=36.65/10.79/0.756/2.49/6.0`；范围重复码 `290.80/139.94/0.483/2.48/7.0`；尖峰+支持 `83.91/9.46/0.909/1.48/1.0`；拥挤未解析 `5.27/2.77/0.189/3.77/1.0`；弱通量 `1.56/0.73/0.534/3.53/1.0`；形状异常 `14.03/12.21/0.252/3.62/3.5`。该结果支持“高亮、重复和像星是三件事”，但只是抽样 detector 证据，不能当作全图真星率。依据为 `doc/02-星图识别/检测器实验记录.md` 8.55、`doc/02-星图识别/亮点真实性与伪影判别研究.md` 10.38，机器表为 `tmp/raw-source-diagnostics-stratified-v2/stratified_raw_source_group_summary.json`。

21. 新增孔径通量泄漏审计：截图双候选的副孔径为 `49` 个像素，与主孔径重叠 `18` 个；重叠区含局部最大值 `13028 ADU`。按副框背景 `36 ADU` 扣除后，副孔径净和 `14,210 ADU` 中 `12,119 ADU`（`85.3%`）来自重叠区，非重叠部分约 `14.7%`。该比例是原始像素污染诊断，不是去混叠通量、独立源概率或伪影率；新字段 `secondary_shared_aperture_*` 已写入 `tmp/local-aperture-overlap-audit-screenshot/source_pair_audit.csv/json`，依据为 `src/rst19/experiments.py` 的 `SourcePairAuditRow` 和 `doc/02-星图识别/检测器实验记录.md` 8.56。重跑还修正了缺少任一独立候选时仍计算双 PSF 的审计边界，修复后成对证据为 `0/15`；累计平移注册对照保存在 `tmp/local-aperture-overlap-audit-screenshot-registered/source_pair_audit.csv/json`，仍为双 PSF `0/15`、附近候选 `15/15`、同源合并 `11/15`。

22. 当前门控版本的单参数敏感性控制显示，`min_distance=4/5/6 px` 的候选数为 `84,594/80,117/73,118`，质量源数为 `29,260/28,374/27,101`；截图两个局部峰在 `4 px` 保留、`5 px` 合并。只改变 `min_flux_snr=3/5/7/10` 时，候选数均为 `84,594`，质量源数为 `41,110/29,260/22,438/16,797`。该控制说明 NMS 是候选竞争参数、SNR 是测量显著性门槛，均不能替代独立 PSF、值域审计、跨帧身份或星表真值；`SNR≥7` 只能作为更严工程口径。依据为当前代码直接重算及 [检测器实验记录 8.57](../02-星图识别/检测器实验记录.md)，暂未写成 precision/FDR 或物理恒星数。
23. 分层强制测光控制：从当前首帧 `source_catalog.csv` 各首要类别按 `flux_snr` 分位点抽取最多 `32` 个源（形状异常 `21` 个），沿 15 帧累计平移在未降噪 FITS 的预测位置强制测量。以序列提案一致的 `peak_x/peak_y` 为锚点，抽样中紧凑质量对照的固定通量 SNR/稳健跨帧 CV/支持像素中位数为 `11.42/0.127/5`，范围异常为 `571.24/0.312/7`，尖峰/拥挤为 `2.32/0.469/1` 与 `8.36/0.244/2`；范围异常的质量样式中位帧数仍为 `15`，说明稳定高响应可能来自固定值域结构。另将截图 `ID 82931/82934` 强制追加，二者固定位置均为 `15/15` 质量样式响应，但仍分别受值域异常和孔径泄漏/未分辨近邻限制。新增的 `±1 px` 局部重定位先用三尺度 Gaussian 匹配响应选位置，并同时保留固定与局部值：`compact_quality` 的位移中位数/重定位比例为 `0 px/13.3%`，`crowded_blend` 为 `1.414 px/100%`；截图两 ID 的局部 SNR 中位数分别为 `976.75`、`853.88`，且多数高响应帧向共享结构靠拢。直接取九个孔径 SNR 最大值会产生普遍角点偏差，因此不作为正式选择准则。峰/质心敏感性对照中，拥挤类固定通量 SNR变化约 `−9.5%`，但结论不变。该实验未重新分类、未完整纳入值域/去混叠旗标，独立命令结果写入 `tmp/forced-stability-audit-code-pattern-current-v8-peak/`、`tmp/forced-stability-audit-code-pattern-current-v8-pair-peak/` 与 `tmp/forced-stability-audit-code-pattern-current-v8-centroid/`，不写入检测缓存；只作为 detector-level 反例，不能解释成真星率；依据为 [检测器实验记录 8.58/8.59](../02-星图识别/检测器实验记录.md)。
24. 针对上述 pair 追加相对几何控制：固定峰距 `4.123 px`，局部间距中位数 `2.000 px`；`13/15` 帧收缩、`11/15` 帧两个局部位置同时向对方移动，`2/15` 帧局部偏移相同，连线顺序无翻转。该结果支持“共享混合亮斑/去混叠不足”作为优先解释，但不排除被异常值域污染的真实近邻源；它不输出双星概率，也不替代联合 PSF 或星表唯一匹配。pair 机器表新增 `forced_stability_pair_frame_metrics.csv` 与 `forced_stability_pair_summary.csv`，仍在上述 v8 pair 目录中。
25. 有限自由位置 K=2 PSF 的当前 pair 复核：raw 数据中 K=2 相对 K=1 仅 `9/15` 帧有 BIC 改善，`ΔBIC≥10` 只有 `1/15` 帧；第二分量 SNR 中位数约 `2.09`、最大约 `2.41`，且 `11/15` 帧至少一个拟合位置触及 `±1.25 px` 边界。因此局部模型改善更像在吸收混合亮斑/翼部结构，不能作为双星确认。机器表为 `tmp/local-free-multipsf-audit-current-pair-v1/local_free_multipsf_audit.csv/json`，依据为检测器实验记录 8.61 和真实性研究 10.43。
26. 新增 4×4 detector 空间分布控制：`linear_artifact` 的 `96/96` 个首帧候选集中在同一网格，而 `compact_quality` 与 `crowded_blend` 分别覆盖 `16/16` 个网格，最大单格比例约 `8.0%/9.5%`。该结果只用于把线状响应分流到读出/线几何审计、把拥挤响应分流到空间 PSF/去混叠审计，不提供伪影率或真星率；机器表为 `tmp/source-quality-audit-code-pattern-current-v3/source_feature_spatial.csv`，依据为检测器实验记录 8.62 和真实性研究 10.44。
27. 经验 PSF pair 形状复核：首帧完整隔离条件只得到 `20` 个模板源（中位 FWHM `2.4966 px`、核和 `3.6465`）；`ID 82931/82934` 在 15 帧注册预测位置上的全局相关度中位数为 `0.560/0.403`，相对残差为 `0.828/0.915`，两个目标均 `15/15` 帧未同时达到相关度 `0.80`。pair 所在 `4×4` 单元没有足够局部隔离模板，因此 local PSF 为 `None`；这表示校准适用域不足，不表示物理上必假。该结果与 `4.123 px` 峰距、`2.74 px` 质心距、`85.3%` 孔径共享、`13/15` 帧相对收缩和自由双 PSF 无确认相互一致；机器产物为 `tmp/empirical-pair-psf-audit-code-pattern-current-v1/empirical_pair_psf_audit.csv/json`，依据为检测器实验记录 8.63、真实性研究 10.45 和 `src/rst19/empirical_pair.py`。
28. 跨帧共变和值域共位复核：`82931/82934` 的固定/局部 `flux_snr` Pearson 为 `0.980/0.990`，按其它 `244` 个完整源的逐帧中位数归一后为 `0.979/0.990`；`29,646` 个控制 pair 的归一化 Pearson P95 为 `0.557/0.621`，目标上尾比例 `1.02%`。但按 pair 总响应中位数分层后，低响应 `7` 帧的组内相关为 `0.991/0.991`，高响应 `8` 帧降为 `0.265/0.151`。两个 `r=4 px` 孔径 `15/15` 帧共享重复码、`9/15` 帧共享 `≤−1000 ADU` 负异常；这支持共享局部结构/值域状态，而不是两颗独立星的充分证据。机器产物为 `tmp/pair-flux-covariance-audit-code-pattern-current-v1/` 和 `tmp/temporal-code-audit-pair-82931-82934-current-v2/`，依据为 `src/rst19/pair_flux_covariance.py` 与检测器实验记录 8.64。
29. 经验 PSF 锚点敏感性控制：同一窗口/掩膜下，质心锚点固定 K=2 的 raw 结果有 `11/15` 帧达到 `ΔBIC≥10` 且第二分量 `SNR≥5`，但 K=1 在双源中点周围 `±2 px`、步长 `0.5 px` 网格内择优后为 `0/15`；峰值锚点固定与控制均为 `0/15`。显式屏蔽 `3990--3993` 后质心固定仍为 `11/15、12/15、11/15`（不同组合），控制仍为 `0/15`。因此前者是单源位置锚定/非高斯残差的选择偏差警报，不是双星确认；实现和产物为 `src/rst19/empirical_pair_fit.py`、`tmp/empirical-pair-fit-code-pattern-current-centroid-v2/` 与 `tmp/empirical-pair-fit-code-pattern-current-peak-v2/`，依据为检测器实验记录 8.65。
30. 各类特征的经验 PSF 留一法：首帧八个非空首要类别各抽取 `16` 个源，逐源排除自身后重建最多 `20` 个模板；`127/127` 个有效成对比较的相关度/残差没有存储精度内变化。留一相关度中位数为紧凑质量 `0.862`、范围异常 `0.749`、边缘/掩膜 `0.683`、尖峰/支持不足 `0.576`、弱背景 `0.556`、线状 `0.412`、形状异常 `0.270`、拥挤未分辨 `0.156`；八个非空类别均成功建立留一模板，边缘/掩膜类有 `1/16` 个裁剪指标无效。该结果支持类别形状排序不是单个模板自相似造成，但不提供真星率或伪影率；实现为 `src/rst19/feature_psf_leaveout.py`，机器产物为 `tmp/feature-psf-leaveout-code-pattern-current-v1/`，依据为检测器实验记录 8.66。
31. 各类特征三层证据矩阵：合并 `source_feature_morphology.csv`、`feature_cross_audit.csv` 和 `feature_psf_leaveout.csv`，把同类质量比例、候选 `≥12/15` 帧持久比例、质量层邻域响应和留一 PSF 相关度/残差保持分列。紧凑质量类为 `1.000/0.946/0.815/0.862/0.507`，形状异常类为 `0/0.952/0.048/0.270/0.963`，拥挤未分辨类为 `0/0.562/0.0066/0.156/0.988`。矩阵另保存候选/质量的同类响应比例，用来控制邻域跨类混入；例如候选同类响应为紧凑质量 `0.788`、拥挤 `0.501`、形状异常 `0.438`、线状 `0.067`。其中质量层邻域响应和同类响应都不等价于真阳性率；结果支持“候选重复不等于真星”，但不输出恒星概率或伪影率。`corr` 线 `0.7/0.8` 的八类分流完全一致，`0.9` 只改变紧凑质量标签，说明 `0.8` 是工程线而非通用真星判据。实现为 `src/rst19/feature_evidence_matrix.py`，机器产物为 `tmp/feature-evidence-matrix-code-pattern-current-v1/`，依据为检测器实验记录 8.67。
32. 将 `source_feature_psf_spatial.csv` 以可选 `--psf-spatial` 合并进类别矩阵，专门控制空间 PSF 替代解释：`2×2`、每类 `16` 个样本中，紧凑质量/线状/拥挤三类局部有效分别为 `11/16、16/16、11/16`，相关度由 `0.862/0.412/0.156` 变为 `0.837/0.405/0.172`，残差由 `0.507/0.911/0.988` 变为 `0.547/0.914/0.985`。局部模板没有使紧凑类稳定改善，线状类仍不符合点源形状，拥挤类的微小变化不足以消除混合残差；该控制仍不是空间变 PSF 的最终标定。实现为 `src/rst19/feature_evidence_matrix.py`，机器产物为 `tmp/feature-evidence-matrix-code-pattern-current-v2-spatial/`，依据为检测器实验记录 8.67。
33. 类别矩阵新增基于实际分母的描述性 Wilson 95% 区间：形状异常候选持久 `20/21` 的区间约为 `[0.773,0.992]`，紧凑质量 `25,056/26,483` 的区间约为 `[0.943,0.949]`；区间不覆盖共享背景、配准和 detector 系统误差，不是恒星真值的置信区间。实现为 `src/rst19/feature_evidence_matrix.py`，依据为检测器实验记录 8.67。
34. 真实背景分层注入机制对照：当前 `2 trials × 4 sites`、峰值超额 `24/56/128 ADU` 下，blank 候选/质量回收为 `0/0、0.875/0、1.0/0.625`，edge 为 `0/0、1.0/0、1.0/0`，crowded 只有最高档为 `0.625/0.625`；line 层局部 RMS 约 `602.5 ADU`，`Q_peak` 约 `0.04/0.09/0.21`，special-code 层无严格无歧义分母。该证据支持局部 RMS、有效孔径、混合、值域和线状结构分流，不能推出整幅 precision、完备率或统一 SNR 阈值；机器产物为 `tmp/stratified-real-background-code-pattern-current-t2-s4/stratified_real_background_injection.csv`，依据为检测器实验记录 8.68。
35. 多次重复特征注入机制控制：首轮 `16` 个随机背景种子已作为 pilot 留存；扩展到当前 `64` 个种子后，紧凑/弱/宽形状/窄形状正样本的候选回收均为 `64/64`，弱和窄的质量回收为 `0/64`；3 px 双源候选/质量一对一回收为 `64/128`，6 px 双源为 `128/128`；掩膜、边缘和饱和为候选 `64/64`、质量 `0/64`。单像素尖峰阴性邻域候选均值/最大值为 `1/1`，长线阴性为 `15.859/19`，质量泄漏均为 `0`。正样本区间是已知注入回收的描述性 Wilson 区间，阴性样本没有 truth 分母；因此该结果支持机制分流，不推出真实 precision、完备率、伪影概率或恒星数。实现为 `src/rst19/feature_audit_replicates.py`，当前机器产物为 `tmp/feature-audit-replicates-code-pattern-current-v2/feature_audit_replicates.csv/json`，依据为检测器实验记录 8.69/8.70。

36. 真实首帧符号反相阴性对照：在 `B=21.0 ADU` 上构造 `I_mirror=2B-I`，保持 `hybrid`、`4σ`、`FWHM=2 px`、`min_distance=4 px`、孔径半径 `4 px`、`flux SNR≥5` 和 `PSF support≥3` 不变，正向/反相得到候选/质量 `84,594/29,260` 与 `2,309/1`。原始候选级比值 `0.027295`、质量级比值 `3.42×10^-5` 仅是 signed-tail leakage 诊断，不是 FDR、p 值或 precision。反相唯一质量源坐标约为 `(1624.7,2606.7)`，其原图孔径含 `≤−29,490.3 ADU` 的极端负码；原始异常像素 `37` 个，按半径 `4 px` 扩张后有 `30` 个反相候选相交、其中 `1` 个质量通过，排除该值域转移后的有效质量泄漏为 `0/29,260`。该对照只约束近似对称噪声尾部，正向热像素/宇宙线/亮线等仍需独立阴性审计。实现为 `src/rst19/signed_null.py`，机器产物为 `tmp/signed-null-audit-code-pattern-current-v1/signed_null_summary.json`、`signed_null_thresholds.csv` 和 `signed_null_feature_counts.csv`，依据为检测器实验记录 8.71。

37. 15 帧符号反相坐标回查：在相同参数下逐帧运行反相对照，pooled 正向/反相候选与质量为 `1,282,630/441,619` 与 `36,170/35`；原始极端负码扩张区相交为 `570/19`，排除后剩 `16` 个反相质量源。35 个反相质量源中 `32/35` 含原始负值，`32/35` 含 `≤−1000 ADU` 异常，`19/35` 含极端负值，`22/35` 含精确 `-1`；排除极端负码后仍有 `13/16` 含异常负值，另有 `3/16` 为无负值证据的弱/宽响应。按质心跨帧 `≤1 px` 连接得到 `6` 个复现簇，其中 `(368.70,946.73)` 的 `3/3` 和 `(1630.70,819.26)` 的 `2/2` 是排除转移后仍复现的固定坐标响应。该结果把反相质量源分为极端负值转移、原始负值/固定哨兵异常、普通弱/宽响应三层；它提高了值域/固定结构解释的优先级，但不提供硬件机理、真星率或伪影率。实现为 `src/rst19/signed_null_sequence.py`，机器产物为 `tmp/signed-null-sequence-code-pattern-current-v2/`，依据为检测器实验记录 8.72。
38. 15 帧反相质量源的机器证据层标签：在 v3 源表中增加互斥 `raw_evidence_layer`，保持 v2 的所有检测数不变；实际分布为 `extreme_range_transfer=19`、`raw_negative_anomaly=13`、`no_negative_evidence=3`，没有独立 `fixed_sentinel_candidate` 或 `raw_negative_other` 质量源。标签只概括原始孔径的主证据，负值/极值/`-1` 计数仍分别保留，不能被解释成噪声概率、硬件故障诊断、伪影率或真星率。实现为 `src/rst19/signed_null.py` 与 `src/rst19/signed_null_sequence.py`，机器产物为 `tmp/signed-null-sequence-code-pattern-current-v3/signed_null_sequence_quality_sources.csv`，依据为检测器实验记录 8.73。

39. 15 帧反相质量源与正向近邻交叉：按当前 GUI 正向参数重新逐帧检测，并在相同原始 detector 坐标查询 35 个反相质量源的正向邻域；正向候选在 ≤1/2/4 px 内为 0/35、23/35、32/35，正向质量源 ≤4 px 为 0/35。32 个 ≤4 px 候选近邻的最近类别为 range_anomaly=21、weak_or_background=11，且全部 quality_passed=False；3 个无局部候选源的最近紧凑质量源距离为 6.89--13.39 px。该结果只是在 detector 坐标上的局部对应控制，支持“邻近候选不等于正常星”，不提供星表身份、precision 或真星率；实现为 src/rst19/signed_null_forward_overlap.py，正式 CLI 机器产物为 tmp/signed-null-positive-overlap-code-pattern-current-v2/signed_null_forward_overlap.csv/json，依据为检测器实验记录 8.74。

40. 首帧最近邻间距群体控制：从当前完整 `84,594` 个候选分别计算测量质心和整数峰的最近其它源距离，并将 `82931/82934` 放回全量分布。该 pair 的质心距 `2.738 px` 位于经验累计分位 `0.0165%`，峰距 `4.123 px` 位于 `5.70%`；`crowded_blend` 的质心最近邻中位数 `4.179 px`，`compact_quality` 为 `7.407 px`。因此 `min_distance=4 px` 只能解释提案几何，质心异常收缩才是混叠/共享结构的补充信号；endpoint count 不是无序 pair 数，结果不提供双星概率、星表身份或物理真值。实现为 `src/rst19/source_separation_audit.py`，机器产物为 `tmp/source-separation-audit-code-pattern-current-v1/source_separation_summary.json`、`source_separation_thresholds.csv`、`source_separation_classes.csv` 和 `source_separation_targets.csv`，依据为检测器实验记录 8.75。

41. 公开 `StarImage_SimuIden` 路线核对：其 [GitHub README](https://github.com/Aoyeww/StarImage_SimuIden) 将功能分为 MATLAB App 中的灰度阈值/连通域/质心提取、基于角距匹配分数转移的图识别、亮星估计 PSF/点目标增强以及运动目标检测跟踪，并列出 [2024 IEEE Sensors Journal 论文](https://doi.org/10.1109/JSEN.2024.3350089) 与 [2025 IEEE TIM 论文](https://doi.org/10.1109/TIM.2025.3573015)。公开页面主要是 README 和 App 安装包，不能据此宣称实现等价或把其输出当作当前 FITS 真值；本文吸收的是职责分层，将固定灰度阈值作为对照，将 PSF、角距匹配和时序跟踪分别放在候选检测之后，并继续保留局部 RMS、值域异常、候选/质量分层和注入回收边界。
42. 星表分配器的局部竞争控制：现有匹配器原先按候选边残差排序后贪心占用检测源和目录源；构造两个检测源、两个目录源的半径门控局部反例时，贪心只返回 `1` 个匹配，而全局线性分配返回 `2` 个匹配。当前默认已改为按稀疏候选连通分量进行“最大匹配数优先、总残差次之”的分配，`greedy` 仍可复现旧基线，GUI/JSON 记录 `assignment_mode`。该控制只说明算法层可能少匹配，不说明当前 FITS 的星表身份、WCS 正确性或双源真值；实现和回归测试为 `src/rst19/matching.py`、`tests/test_matching.py`。
43. 类别证据矩阵增加确定性复核路由：在保留形态、候选/质量邻域响应、留一 PSF、空间模板和 Wilson 描述性区间的基础上，`recommended_audit_stage` 将范围异常、线状、边缘、拥挤、尖峰、弱源、形状异常和紧凑质量分别路由到值域、线几何、有效孔径、联合 PSF、二维支持、局部噪声/注入、形状 PSF 和星表/WCS 队列；`counting_policy` 只把 `compact_quality` 标为可进入身份核验队列，其余保留候选、不直接计星。该字段是固定规则，不加权、不产生恒星概率、不修改默认质量层；复现产物为 `tmp/feature-evidence-matrix-code-pattern-current-v3-routing/feature_evidence_matrix.csv/json`，实现为 `src/rst19/feature_evidence_matrix.py`，测试为 `tests/test_feature_evidence_matrix.py`。
44. 82931/82934 独立像素支持留出：新增局部裁剪重检和几何孔径分区，按最大基数/最小残差一对一分配，避免一个局部候选重复计给两个目标。15 帧 raw 同时分配两个候选为 `4/15`，两个局部质量同时通过为 `0/15`；屏蔽重复码 `3990--3993` 后为 `0/15`，屏蔽局部 `≤−1000 ADU` 异常后也为 `0/15`。首帧质心孔径共同区为 `29` 个像素，正残差占比为 `93.7%/98.6%`，15 帧中位数为 `94.5%/98.8%`。共同孔径屏蔽后仍可提出外缘候选，但均带 `MASKED`，不能当作双星确认；该审计明确区分整数峰 `18/49` 与质心 `29/50--51` 的分母，不改默认检测/质量层或 GUI。实现为 `src/rst19/pair_support_audit.py`，测试为 `tests/test_pair_support_audit.py`，产物为 `tmp/pair-support-audit-code-pattern-current-v2/pair_support_audit.csv/json` 与 `pair_support_summary.csv`，依据为检测器实验记录 8.77。

45. 82931/82934 孔径敏感性控制：在相同 15 帧和 detector 口径下将孔径半径改为 `3/4/5 px`，raw 双分配均为 `4/15`、双质量均为 `0/15`，重复码/局部异常屏蔽后的双分配均为 `0/15`；共享正残差占比中位数为 `90.5%/97.9%/99.1%`。该结果排除 4 px 单点参数的简单解释，但确认共享比例是几何测光诊断量，不是伪影概率；产物为 `tmp/pair-support-audit-code-pattern-current-r3/`、`current-v1/`、`current-r5/`，依据为检测器实验记录 8.78。

46. 空间分区留出注入与信号边界：在真实首帧 `4096×4096` 图像划分 `2×2` detector 单元，每格从当前候选、特殊值和局部亮结构之外选择 1 个相对空白位置，以统一 Gaussian `FWHM=2 px` 和离散积分超额注入。首帧原始基线为 `84,594/29,260` 候选/质量；`256/512/1024 ADU` 三档的候选回收均为 `4/4`，质量回收为 `0/4、4/4、4/4`。逐源复核中，`256 ADU` 的 `flux SNR=2.35--3.57`、`filter SNR=7.43--10.05`，四源均仅因 `LOW_FLUX_SNR` 被拒；`512 ADU` 的 `flux SNR=5.89--7.52`、PSF 支持 `7--9`，四源均通过。`128 ADU` 先导档候选为 `3/4`、质量为 `0/4`，对应新增峰的通量 SNR 约 `0.37/1.26/1.59`，因此不能把低信号质量失败写成空间失效。配对版增加同 `float32`、同参数、无注入控制：三个单元相对控制增加 `+1` 候选，`r1c1` 增加 `+16`，但质量只增加 `+1`；额外候选集中在 `filter_snr≈4` 边界，伴随全图 `filter_noise` 约 `0.045%` 的重估变化，属于宽筛的非局部归一化耦合，不是 15 个特殊星点。该 pilot 没有发现四个相对空白分区的系统性质量失效，但样本和统一 Gaussian 核不足以标定空间 PSF 或正式完备率；实现为 `src/rst19/spatial_injection.py`，测试为 `tests/test_spatial_injection.py`，产物为 `tmp/spatial-injection-code-pattern-current-2x2-gaussian-t1/`、`tmp/spatial-injection-code-pattern-current-2x2-gaussian-t1-high/` 与 `tmp/spatial-injection-code-pattern-current-2x2-gaussian-t1-high-paired-v2/`，依据为检测器实验记录 8.79 与真实性研究 10.54。

47. 经验 PSF 孔径敏感性与当前特殊亮点的测光边界：从首帧 20 个孤立源构造逐峰归一经验模板，在固定的四个 `2×2` detector 空间位置分别注入 `512 ADU` 离散积分信号，扫描 `r=3/4/5/6 px`，每档保存同 dtype 无注入配对控制。四个注入源候选回收均为 `4/4`，质量回收分别为 `4/4、2/4、0/4、0/4`，`flux SNR` 中位数为 `6.428、5.168、3.940、3.644`；模板在相应孔径内的离散包围能量为 `77.35%/80.94%/85.42%/90.98%`。该实验说明候选可见、测光可靠和物理身份不能合并判断；经验模板的外翼与背景噪声会共同改变质量门结果，不能把回收率当作真实完备率，也不足以单独修改默认孔径。实现为 `src/rst19/aperture_sensitivity.py`，测试为 `tests/test_aperture_sensitivity.py`，依据为检测器实验记录 8.80 与真实性研究 10.55。

48. 82931/82934 局部 PSF 适用域扫描：固定 `support_radius=7 px` 和 `21 px` 隔离半径，扫描 pair 所在 `4×4` 网格单元的局部模板窗口。`18/64/128/256/512 px` 的候选池为 `6,081/6,939/8,178/10,803/17,132`，质量可用池为 `1,821/2,088/2,461/3,240/5,185`，但每档真正满足隔离条件的源都只有 `2` 个，因而不足以建立最少 3 源的局部 PSF；`1024 px` 才得到 `6` 个宽范围模板源。该结果把 `local_psf=None` 定义为拥挤区校准缺口，而不是伪影或无星证明；宽范围 fallback 的主/副相关度中位数为 `0.553/0.480`、相对残差为 `0.833/0.877`，只作为敏感性证据。依据为检测器实验记录 8.81 与真实性研究 10.56。

49. 类别级持续率—PSF 形状解耦：对类别证据矩阵的 8 个有效首要类别比较候选 `≥12/15` 帧持续率与逐源留一 PSF 相关度中位数，Spearman `ρ=0.048`、Pearson `r=0.018`。`shape_outlier` 是高持续/低相关反例（`0.952/0.270`），`range_anomaly` 是低持续/较高相关反例（`0.091/0.749`）；类别数只有 8 个，因此仅作描述性机制证据，不作总体统计推断。依据为检测器实验记录 8.82 与真实性研究 10.57。
50. 参数敏感性与类别转移：首帧固定 `hybrid/FWHM=2/min_distance=4`，`flux SNR=3/5/7/9` 的候选数均为 `84,594`，质量数为 `41,110/29,260/22,438/18,296`；候选阈值 `4/5/6/8σ` 的候选数为 `84,594/73,448/65,092/53,371`。提高 `flux SNR` 到 9 时，`9,614` 个默认 `compact_quality` 位置转为 `weak_or_background`；降低到 3 时，`10,280` 个默认弱/背景位置转为紧凑类。这是同坐标质量重标记，不是物理恒星增删；实现/测试为 `src/rst19/feature_parameter_sensitivity.py`、`tests/test_feature_parameter_sensitivity.py`，产物为 `tmp/feature-parameter-sensitivity-code-pattern-current-v1/`，依据为检测器实验记录 8.83 与真实性研究 10.58。

51. 15 帧参数对照与双框复核：将已有 `flux SNR=3/5/9` 的序列 JSON 在相同 `15` 帧、`≥12/15`、`1 px` 注册邻域下比较，三组候选数均为 `84,594`，质量数为 `41,110/29,260/18,296`，`compact_quality` 质量持久率为 `0.776/0.815/0.864`；`82931/82934` 在三组中都分别保留 `CODE_PATTERN` 与 `UNRESOLVED_BLEND`、质量不通过、候选位置仅 `4/15` 帧，副响应同诊断子组为 `3/15`。这把“质量门改变稳定子集”和“截图双框的异常/混叠机制”分开；它不是双星真值或噪声概率。比较器为 `src/rst19/sequence_feature_comparison.py`、命令入口为 `src/rst19/feature_sequence_compare_cli.py`、测试为 `tests/test_sequence_feature_comparison.py`，产物为 `tmp/sequence-feature-parameter-comparison-current-v1/`，依据为检测器实验记录 8.84 与真实性研究 10.59。

52. 类别内经验分位 hard-negative 审计：新增 `rst19-feature-class-context`，从 `source_catalog.csv` 中把指定 detection ID 放回其首要 `feature_class` 的经验分布，输出 `flux_snr/filter_snr/fwhm/ellipticity/sharpness/psf_support/footprint/centroid_shift/peak` 的 `p10/median/p90` 和经验分位。`82934` 在 `crowded_blend` 的 `905` 个候选中，通量 SNR、滤波 SNR、PSF 支持分别约为 `98.2%/99.9%/99.6%` 分位；`82931` 在 `range_anomaly` 类的 PSF 支持为 `100%` 分位，但两者仍分别带 `UNRESOLVED_BLEND/CODE_PATTERN`、质量不通过。该结果证明高显著性与独立身份可以冲突，经验分位只用于相对定位，不是真星概率；实现为 `src/rst19/feature_class_context.py`、入口为 `src/rst19/feature_class_context_cli.py`、测试为 `tests/test_feature_class_context.py`，产物为 `tmp/feature-class-context-pair-82931-82934-current-v1/`，依据为检测器实验记录 8.85 与真实性研究 10.60。

53. 固定码关注坐标的逐帧值域复核：扩展 `rst19-temporal-codes` 输出 `temporal_code_focus_series.csv`，记录 pair 关注点 `(2439,4021)` 和相邻 `(2437,4021)` 在 15 帧的原始值。前者为 `3990--3993` 且 `15/15` 帧在该范围、跨度 `3 ADU`；后者精确 `-1` 为 `2/15`、`≤−1000 ADU` 为 `9/15`，并出现 `-21691/-28312/23380` 等极端值。它把“固定/低变化 detector 结构”和“普通逐帧星光响应”分开，支持值域/固定结构参与双框的优先解释；但由于 `BLANK`、signedness、ADC 满阱和坏像素语义未确认，不能命名为已证实坏点或溢出。实现字段在 `src/rst19/experiments.py`，回归覆盖 `tests/test_experiments.py`，产物为 `tmp/temporal-code-audit-pair-82931-82934-current-v2/`，依据为检测器实验记录 8.86 与真实性研究 10.61。

54. 类别内高显著性落选扫描：新增 `rst19-feature-hard-negative`，只读取完整 `source_catalog.csv`，对 8 个有效 `feature_class` 中 `quality_passed=False` 的行按 `flux_snr` 排名，并默认输出每类 top 5。最高落选值分别为线状 `1863.2`（`LINE_ARTIFACT`）、范围异常 `936.3`（`NEGATIVE_OVERFLOW`）、边缘/掩膜 `612.4`、拥挤 `540.6`（`UNRESOLVED_BLEND`）、尖峰/支持 `133.9`（支持像素 `1`）和弱/背景约 `5.0`（`LOW_FLUX_SNR`）。这证明“高 SNR 仍被拒”跨类别存在，但拒绝原因不同，不能合成一个噪点率或真星概率；实现为 `src/rst19/feature_hard_negative.py`，测试为 `tests/test_feature_hard_negative.py`，产物为 `tmp/feature-hard-negative-code-pattern-current-v1/`，依据为检测器实验记录 8.87 与真实性研究 10.62。

55. hard-negative 跨帧代表复核：从类别内最高 `flux_snr` 落选源中显式追加 `64963/4469/42/39547/25028/23168/66157`，再加每类一个分层对照，使用 15 帧累计平移做原始图固定/局部强制测光。`39547` 固定通量 SNR 中位 `863.1`、质量样式 `15/15`，`64963` 固定/局部为 `546.5/466.5` 且局部重定位 `15/15`，`4469` 首帧 `1863.2` 但固定位置中位 `5.4`，`23168` 固定支持中位 `1` 且质量样式 `0/15`，`25028` 固定/局部 `90.4/192.6` 且 `15/15` 重定位，`66157` 两种位置均约 `5.2`。这把高 SNR 落选拆成值域稳定、混合移动、线状不持久、支持不足和弱背景边界等机制；`quality_like` 只是强制测量诊断，不覆盖原始 `quality_passed`。实现复用 `src/rst19/forced_stability.py`，产物为 `tmp/feature-hard-negative-forced-stability-current-v1/`，依据为检测器实验记录 8.88 与真实性研究 10.63。

56. 三种 SNR 的分歧模式：在 hard-negative top-1 中，`crowded_blend` 的 `filter_snr/flux_snr=0.28`，`range_anomaly=3.90`，`masked_or_edge=4.18`，`linear_artifact=0.88`，`spike_or_support=1.41`，`weak_or_background=3.69`；同时尖峰代表 peak 为 `2411` 但 PSF 支持仅 `1`，线状代表 FWHM 为 `4.22 px`。这些值不是可比较的真星分数，而是孔径通量、PSF 匹配和峰值统计的分歧诊断；它支持按机制分流，不支持单一 SNR 加权。`feature_hard_negative_rows.csv/json` 新增 `filter_flux_snr_ratio`，依据为检测器实验记录 8.89 与真实性研究 10.64。

57. 空间条件化 hard-negative 审计：新增 `rst19-feature-spatial-context`，在 `4×4` detector 网格上同时记录类别最大单元占比、归一化空间熵、边缘 `≤16 px` 比例和 `flux SNR≥10` 落选热点，并对 top-5/显式目标输出最近候选与 `5/10/20 px` 邻域类别。当前 `linear_artifact` 为 `96/96` 同格，`crowded_blend` 与 `compact_quality` 覆盖 `16/16` 格、最大单格约 `9.5%/8.0%`；截图 `82931/82934` 的边缘距离 `73.8/73.2 px`、最近候选距 `2.738 px`，5 px 内只有互相响应。该结果把 pair 从边缘机制中分离出来，支持局部混合优先解释，但不输出伪影率、双星概率或 WCS 身份；实现为 `src/rst19/feature_spatial_context.py`，测试为 `tests/test_feature_spatial_context.py`，产物为 `tmp/feature-spatial-context-code-pattern-current-v1/`，依据为检测器实验记录 8.90 与真实性研究 10.65。

58. 宽范围经验 PSF 的模板敏感性复核：从 `1024 px` 范围内的 `6` 个隔离源重建经验 PSF（中位 FWHM/椭圆率 `2.594/0.066`），对 `82931/82934` 复跑相同 15 帧、质心锚点和掩膜条件。raw 固定双源工程证据线为 `11/15`，但双源中点周围最佳单源位置控制为 `0/15`；在同一研究性网格控制中，最佳单源 BIC 始终比固定双源低 `56.4--688.4`、中位 `552.2`，首帧固定双源 `ΔBIC=-6.31`、第二分量 SNR `0`。网格差值含位置试验的多重比较，只作锚点公平性控制，不是正式自由位置显著性；它把局部模板缺口与两个框的成因进一步分开，不能写成双星确认。研究复用 `src/rst19/empirical_pair_fit.py`，产物为 `tmp/empirical-pair-fit-local-wide-1024-pair-82931-82934-current-v1/`，依据为检测器实验记录 8.91 与真实性研究 10.66。
59. 有界自由位置经验 PSF 与退化控制：新增 `rst19-empirical-pair-free`，在同一经验 PSF、窗口、背景和掩膜下连续优化 K=1/K=2 的位置和幅度，并允许 K=2 两个分量从同一位置起步。对 `82931/82934` 使用 K=1 `±2 px`、K=2 `±3 px` 边界，raw 15 帧联合证据线通过为 `0/15`；raw 拟合间距中位约 `0 px`、最大约 `1.05 px`，F13 的正 BIC 改善伴随 `0.107 px` 间距和约 `1.0×10^-6` 第二分量 SNR，属于退化解。重复码/范围掩膜组合也无通过帧；同一实现的 `6 px` 双源注入回归控制可以通过联合线，说明当前 pair 的失败不是算法完全失去双源能力。实现为 `src/rst19/empirical_pair_free.py`，入口为 `src/rst19/empirical_pair_free_cli.py`，测试为 `tests/test_empirical_pair_free.py`，产物为 `tmp/empirical-pair-free-code-pattern-current-v3/`，依据为检测器实验记录 8.92 与真实性研究 10.67。
60. 观测几何双源注入控制：在同一首帧真实背景、`hybrid + 4σ + FWHM=2 px + min_distance=4 px` 下，每条件 `1 trial × 8` 对，扫描总峰值 `1024/4096 ADU`、副/主峰比 `0.143/0.25/1`。在整数峰距 `4.123 px`、比值 `0.143` 时，Gaussian 双源候选解析为 `1.0`、质量解析为 `0.5`；经验 PSF 双源候选/质量解析为 `0.875/0.25`（1024）和 `1.0/0.25`（4096），合并率均为 `0`。把间距改为观测质心距 `2.738 px` 后，Gaussian 双源候选/质量解析均为 `0`、合并率为 `1.0`。这支持“整数峰距越过 NMS 线会触发两个宽筛响应，但质心仍在混叠区”的机制解释，不支持把两个框直接写成两颗恒星；注入位点相对干净，不能代替污染背景、空间 PSF 或星表/WCS 真值。产物为 `tmp/pair-flux-ratio-observed-geometry-gaussian-current-v1/`、`tmp/pair-flux-ratio-observed-geometry-empirical-current-v1/` 和 `tmp/pair-flux-ratio-observed-centroid-geometry-gaussian-current-v1/`，依据为检测器实验记录 8.93 与真实性研究 10.68。
61. 原始像素拓扑审计：以两个候选的共同背景/噪声和 `10 px` 局部窗口做 8 邻接正响应连通与 raw 局部极大值检查。`3/5/8/10σ` 下两个候选均在同一连通块，连通块像素数 `50/41/32/32`；`3/5/7 px` raw 局部极大值口径下两个候选均为 `0/3` 次局部极大值，最高原始像素为 `(2438,4022)=13028 ADU`。排除重复码后主候选 `3992 ADU` 峰离开正响应层，副候选仍与中间结构连接。该结果支持共享非高斯结构/值域污染优先解释，但不单独排除真实近邻 PSF；实现为 `src/rst19/pair_pixel_topology.py`，测试为 `tests/test_pair_pixel_topology.py`，产物为 `tmp/pair-pixel-topology-pair-82931-82934-current-v1/`，依据为检测器实验记录 8.94 与真实性研究 10.69。

62. 原始像素拓扑类别控制 pilot：同一首帧按测量质心距 `2.0--3.8 px`、整数峰距 `3.0--6.5 px` 筛选 `compact_quality` 近邻控制 `8` 对；目标 pair 的质心距 `2.738 px`，控制最小为 `3.54 px`。目标 pair 为 `0/1` 对双候选 raw 局部峰，compact 控制为 `7/8` 对双候选 raw 局部峰。该结果只支持目标更靠近混叠/重定位区，控制样本小且几何不完全匹配，不是 precision、噪点率或恒星概率；产物为 `tmp/pair-pixel-topology-controls-current-v1/`。

63. 各类特征非参数效应量：以 `compact_quality` 为参考，对 7 个落选类别和 11 个源级字段计算 AUC/Cliff's delta。代表结果为 `crowded_blend` 的 filter SNR AUC `0.901`、flux SNR `0.629`；`linear_artifact` 的 FWHM `0.041`、sharpness `0.948`、flux SNR `0.103`；`shape_outlier` 椭圆率 `0.000`；`spike_or_support` PSF 支持 `1.000`；`range_anomaly` 异常像素计数 `0.002`；`weak_or_background` flux SNR `1.000`。这些是当前 detector-rule signature，不是独立分类器精度、precision、FDR 或恒星概率；实现为 `src/rst19/feature_effect_size.py`，测试为 `tests/test_feature_effect_size.py`，产物为 `tmp/feature-effect-size-code-pattern-current-v1/`。
64. 污染背景中的已知双源注入：新增 `run_contaminated_pair_injection_audit`，在同一真实首帧的目标 pair 中点、`compact_quality` 源 `22381` 附近和线状候选 `4469` 附近叠加已知双源，固定总峰值 `4096 ADU`、Gaussian `FWHM=2 px`，扫描间距 `2.738/4.123 px` 与副/主峰比 `0.143/1`。目标 pair 在 `2.738 px` 只保持候选双命中而质量不成对，`4.123 px` 才能质量成对；紧凑质量源附近也需 `4.123 px` 才稳定分开；线状结构附近未形成可靠质量双源，且短间距出现候选合并。该实验显式分离 baseline、injected、new 三层，避免把原有候选冒充注入回收；实现为 `src/rst19/contaminated_pair_injection.py`，入口为 `src/rst19/contaminated_pair_injection_cli.py`，测试为 `tests/test_contaminated_pair_injection.py`，产物为 `tmp/contaminated-pair-injection-code-pattern-current-v1/`，依据为检测器实验记录 8.97 与真实性研究 10.72。
65. 跨类别污染双源复核：新增 `rst19-contaminated-pair-class`，从 8 个互斥首要特征类别各确定性选择一个中位 `filter_snr` 锚点，统一扫描 `2.738/4.123 px` 与 `0.143/1`，共 `32` 个条件。`2.738 px` 下候选/质量双命中为 `0/16`、`0/16`；`4.123 px` 下为 `11/16`、`9/16`，但线状和范围异常类均为 `0/4`。这支持把双源可解析性拆成间距—PSF 几何轴和局部结构—质量门轴，不把类别回收率写成物理恒星概率；实现为 `src/rst19/contaminated_pair_class.py`，入口为 `src/rst19/contaminated_pair_class_cli.py`，测试为 `tests/test_contaminated_pair_class.py`，产物为 `tmp/contaminated-pair-class-audit-code-pattern-current-v1/`，依据为检测器实验记录 8.98 与真实性研究 10.73。
66. 跨类别污染双源多锚点扩展：新增 `analysis_scope=local_roi`，每类选择 3 个中位 `filter_snr` 锚点，在 `385×385` ROI 中完成 `96` 个条件；短距候选/质量为 `0/48、0/48`，长距为 `33/48、28/48`，线状与范围异常长距候选均为 `0/12`。同一 `32` 个条件与全幅单锚点版本的 8 个布尔命中字段逐项 `32/32` 一致，说明 ROI 只扩展研究规模，不改变已核验判断；ROI 候选数明确不是全图数量。实现为 `src/rst19/contaminated_pair_injection.py` / `src/rst19/contaminated_pair_class.py`，测试为 `tests/test_contaminated_pair_injection.py` 和 `tests/test_contaminated_pair_class.py`，产物为 `tmp/contaminated-pair-class-audit-code-pattern-current-v2-local-per3/`，依据为检测器实验记录 8.99 与真实性研究 10.74。

67. 端点级质量原因扩展：在不改变 detector 逻辑的前提下，污染双源审计额外保存每个注入端点的检测 ID、匹配距离和质量原因。长距 `4.123 px` 的线状类 `12/12` 端点为 `NO_CANDIDATE`，范围异常类 `6/12` 端点触发 `NEGATIVE_OVERFLOW`，拥挤/形状类的主要残余失败为 `UNRESOLVED_BLEND`，尖峰/支持和弱背景类均为 `12/12 QUALITY_PASS`。v3 与 v2 的 `96` 条条件逐项 `16` 个布尔字段一致；原因层只扩展审计信息，不改变已有结论。实现为 `src/rst19/contaminated_pair_injection.py` / `src/rst19/contaminated_pair_class.py`，产物为 `tmp/contaminated-pair-class-audit-code-pattern-current-v3-local-per3-reasons/contaminated_pair_quality_reason_summary.csv`，依据为检测器实验记录 8.100 与真实性研究 10.75。

68. 源级特征相关性审计：对当前 `84,594` 个候选计算 `peak`、通量/滤波 SNR、FWHM、形状、足迹、PSF 支持和质心偏移的 Spearman 相关性，并按 `feature_class` 输出中位数及类别内相关性。`peak—filter_snr` 为 `0.9355`、`flux_snr—filter_snr` 为 `0.8711`、`filter_snr—FWHM` 为 `−0.8444`；类别内 `compact_quality` 为 `−0.865`、`crowded_blend` 为 `−0.589`，而 `linear_artifact/range_anomaly` 为 `0.164/0.184`。因此多个显著性量有重复信息，但不同机制不能套一个宽度模型，也不能相加成真星分数。该结果是 detector-rule 依赖审计，不是因果、分类器性能或物理概率；实现为 `src/rst19/feature_correlation_audit.py`，测试为 `tests/test_feature_correlation_audit.py`，产物为 `tmp/feature-correlation-audit-code-pattern-current-v1/`，依据为检测器实验记录 8.101。
69. 伪影识别的文献边界补强：Desai et al. 2016 使用 PSF 同质化参考图、单帧位置变化 PSF 和多帧比较识别宇宙线、卫星轨迹、散射光、饱和峰和 bleed trail。该证据支持本项目把线状/尖峰/饱和邻域从点源质量路由中分开，但不直接证明开运一号的具体亮点属于同类伪影；本文仍将其作为实验设计边界，而非物理真值。来源为 [Desai et al. 2016](https://arxiv.org/abs/1601.07182)，对应真实性研究 10.77。
70. 格式层机器复核：新增 `rst19-format-audit`，对 15 个 FITS 检查实际尺寸、文件长度、`BITPIX`/dtype、`BSCALE/BZERO/BLANK`、首行辅助区域、标准 WCS 卡片、负值/`-1`/正负极值，并用 `DATE-OBS` 对位置—速度字段做差分自洽检查。结果为 `15/15` 个 `4096×4096` 大端 `>i2` 文件，文件长度全部匹配，`BLANK=0/15`，精确 `-1` 合计 `2,185`，标准 WCS 卡片和最小 WCS 核心均为 `0/15`；J2000/WGS84 速度相对误差中位分别为 `4.61×10^-5/4.79×10^-5`。该结果支持读取与单位解释边界，不确认 ADC signedness、坏像素/饱和语义或完整 WCS；实现为 `src/rst19/format_audit.py`，测试为 `tests/test_format_audit.py`，产物为 `tmp/format-audit-code-pattern-current-v1/`，依据为本地格式核验第 13 节。
71. 类别条件双源注入统一表：将 `8` 个首要特征类别各取 `3` 个中位 `filter_snr` 锚点，扫描 `2.738/4.123 px` 与 `0.143/1`，形成 `96` 个局部 ROI 条件；短距所有类别候选/质量双命中均为 `0/6、0/6`，长距按类别分化为紧凑 `6/6、5/6`、拥挤 `4/6、2/6`、线状 `0/6、0/6`、范围异常 `0/6、0/6`、形状 `6/6、4/6` 等。端点原因另显示线状 `NO_CANDIDATE`、范围异常 `NEGATIVE_OVERFLOW`、拥挤/形状 `UNRESOLVED_BLEND`。该结果只说明间距—结构对 detector 质量层的影响，不是类别物理真值、完备率或恒星概率；机器产物为 `tmp/contaminated-pair-class-audit-code-pattern-current-v3-local-per3-reasons/`，对应真实性研究 10.79。
72. 64 次随机背景重复机制控制：在保持检测器与注入机制不变的情况下，`broad_fwhm6` 和紧凑孤立正样本候选/质量均为 `64/64`；弱、窄、边缘、掩膜和饱和正样本候选均为 `64/64`、质量均为 `0/64`；3 px 双源一对一候选/质量为 `64/128`，6 px 双源为 `128/128`；单像素尖峰阴性邻域候选均值/最大为 `1/1`，长线为 `15.859/19`，质量泄漏均为 `0`。该结果只支持机制分流对随机背景的稳定性，不推出真实 precision、完备率、伪影概率或物理恒星数；宽形状不能自动判假。实现为 `src/rst19/feature_audit_replicates.py`，产物为 `tmp/feature-audit-replicates-code-pattern-current-v2/feature_audit_replicates.csv/json`，依据为检测器实验记录 8.70 与真实性研究 10.80。
73. 高 SNR hard-negative 机制矩阵：对 `64963/4469/42/39547/25028/23168/66157` 的 15 帧固定位置与 `±1 px` 局部峰强制测光，得到拥挤源固定/局部通量 SNR 中位 `546.5/466.5` 且 `15/15` 帧重定位；线状源首帧 `1863.2` 但固定中位 `5.4`；范围异常 `863.1/837.3` 且固定 `15/15` 质量样式；形状异常 `90.4/192.6` 且 `15/15` 重定位；支持不足固定质量样式 `0/15`、局部 `13/15`；弱源约 `5.2/5.2`。该表把固定值域稳定、局部混合/重定位、线状不持久和通量边界拆开；`quality_like` 与局部峰均为诊断，不是物理恒星身份。机器依据为 `tmp/feature-hard-negative-code-pattern-current-v1/feature_hard_negative_rows.csv`、`tmp/feature-hard-negative-forced-stability-current-v1/forced_stability_frame_metrics.csv` 和 `forced_stability.json`，对应真实性研究 10.81。
74. 注册位移可辨识性边界：15 帧累计 detector 平移最大模长约 `0.179 px`，x/y 范围约 `0.188/0.244 px`，平均模长约 `0.096 px`；关联半径为 `1 px`、PSF FWHM 为 `2 px`。因此 `12/15` 或 `15/15` 持久性无法单独排除 detector 固定结构；下一步需要 dither、暗场/平场、完整 WCS 或 detector 坐标 null。机器依据为 `tmp/sequence-feature-persistence-code-pattern-current-v4/sequence_feature_persistence.json`，对应真实性研究 10.82。
75. 注册与 detector-fixed null 对照：在相同 detector 参数下，正常注册版与零平移版的候选 `≥12/15` 持久数为：紧凑 `25,056→16,272`、弱背景 `16,128→9,259`、尖峰 `17,521→15,761`、边缘 `9,590→7,141`、拥挤 `509→433`、线状 `22→13`、形状 `20→17`、范围异常 `4→1`；质量邻域计数仅紧凑 `21,573→21,559`、边缘 `2,119→2,118` 等轻微变化。该结果是坐标敏感性 null，不证明固定坐标结果为物理真值；它说明候选持久性与质量响应必须分列报告。机器产物为 `tmp/sequence-feature-persistence-code-pattern-current-v4/` 与 `tmp/sequence-feature-persistence-fixed-null-current-v1/`，对应真实性研究 10.83。
76. 当前 v4 类别转移复核：从 `tmp/sequence-feature-persistence-code-pattern-current-v4/sequence_feature_class_transition.csv` 按 `q_any=Σ_{c'}M(c,c')/(15N_c)` 与 `q_same=M(c,c)/Σ_{c'}M(c,c')` 重算类别响应。候选 `q_any/q_same` 为：紧凑 `0.963/0.819`、边缘 `0.870/0.308`、尖峰 `0.827/0.686`、弱背景 `0.846/0.596`、拥挤 `0.732/0.685`、形状 `0.968/0.453`、范围 `0.374/0.599`、线状 `0.293/0.228`；质量层只有紧凑 `0.879/0.914` 和边缘 `0.828/0.222` 有非空质量锚点分母。该结果把“附近有响应”与“仍是同一算法机制”分开：形状/边缘可以高频重复但类别切换明显，范围异常响应少但条件同类率较高；所有比例都是 detector-level 诊断，不是逐星身份、真星概率或伪影率。机器依据为同一转移 CSV，对应检测器实验记录 8.102 和真实性研究 10.84。

77. 双框机制的快速后处理：从 `tmp/source-separation-audit-code-pattern-current-v1/source_separation_classes.csv` 统计各首要类别的 `centroid NN≤4 px` 端点率，得到 `compact_quality 607/26,483=2.29%`、`crowded_blend 216/905=23.87%`、`range_anomaly 11/44=25.00%`、`shape_outlier 6/21=28.57%`，其余为线状 `4/96=4.17%`、边缘/掩膜 `191/11,971=1.60%`、尖峰 `378/23,824=1.59%`、弱背景 `259/21,250=1.22%`。该字段是最近邻端点而非无序 pair、双星概率或伪影率；它把双框风险定位到拥挤/异常/形状类别。对 `82931/82934`，峰距 `4.123 px` 越过 NMS `4 px`，质心距 `2.738 px` 位于全体候选最近邻 `0.0165%` 分位，坐标收缩比 `C=d_centroid/d_peak=0.664`，且两者质量均不通过；这支持“提案分裂与物理独立性分层”的解释。进一步的类别流向由 `src/rst19/feature_transition_flow.py` 从同一转移 CSV 机器汇总，产物为 `tmp/sequence-feature-transition-flow-current-v1/sequence_feature_transition_flow.csv/json`；对应真实性研究 10.86 与检测器实验记录 8.104，测试为 `tests/test_feature_transition_flow.py`。

78. 当前 pair 的提议器来源审计：从 `tmp/source-quality-audit-code-pattern-current-v3/source_catalog.csv` 读取 `82931/82934`，前者为 `gaussian|dog_narrow`、`range_anomaly/CODE_PATTERN`、`filter_snr=1195.0`，后者为 `gaussian`、`crowded_blend/UNRESOLVED_BLEND`、`filter_snr=300.3`；因此在当前混合运行的来源追溯中，第二框不是 DoG/Starlet-only，但这不等价于关闭其他提议器后独立 Gaussian-only 重跑必然得到同样的两个 ID。全量来源摘要中，`compact_quality` 为 `23,848/26,483` 三路交集且有 `286` 个无 Gaussian 质量通过，`crowded_blend` 为 `902/905` DoG-only 且质量通过数为 `0/905`，`masked_or_edge` 为 `7,989/11,971` 三路交集且有 `16` 个无 Gaussian 质量通过，`range_anomaly` 的 `44` 个均含 Gaussian。来源交集是同一原图上的相关提议，不是独立投票；它只用于调度星表/WCS、PSF、值域和局部留出复核。不要把历史截图 `70269/70320` 的 `3991 ADU` 结果与当前 `82931/82934` 混用。对应详细研究为真实性研究 10.87、检测器实验记录 8.105。

79. 孔径通量/峰值比的类别条件审计：从 `tmp/aperture-peak-ratio-audit-code-pattern-current-v1/` 统计 `R=flux/peak`。`compact_quality` 中位数 `2.799`，`crowded_blend` `5.567`，`linear_artifact` `17.304`，`spike_or_support` `1.092`；`82934` 为 `24.974`、处于拥挤类 `92.93%` 分位，`82931` 为 `5.973`、处于范围异常类 `75.00%` 分位。由于线状类天然有较高孔径/峰值比，`R` 只能做类别条件化的孔径污染提示，不能设一个统一删源线；它为高通量 SNR 与独立源身份分离提供补充证据。实现为 `src/rst19/aperture_peak_ratio.py`，对应真实性研究 10.88、检测器实验记录 8.106。

80. `R` 与提议器来源的交叉控制：`compact_quality` 的 `all_three/gaussian_only/dog_only` 中位 `R` 为 `2.706/5.101/6.522`，`crowded_blend` 的 `902` 个 DoG-only 中位为 `5.557`，仅 `2` 个 Gaussian-only 中位为 `20.378`；当前 `82934` 的 `R=24.974`，另一个 Gaussian-only 拥挤候选 `ID 44132` 为 `15.782`。拥挤 Gaussian-only 分母为 `2`，不能作概率推断；该结果只说明来源组合和孔径污染是相关但不等价的证据链，不能重复加权。实现产物为 `aperture_peak_ratio_method_summary.csv` 和 `aperture_peak_ratio_targets.csv`，对应真实性研究 10.88 的“来源组合与 `R` 的交叉控制”。

81. 质量旗标按类别分解：从 `tmp/feature-flag-profile-code-pattern-current-v1/feature_flag_profile.csv` 统计，`crowded_blend` 的 `905/905` 带 `UNRESOLVED_BLEND`，`weak_or_background` 的 `21,250/21,250` 带 `LOW_FLUX_SNR`，`linear_artifact` 的 `96/96` 带 `LINE_ARTIFACT`，`spike_or_support` 的 `23,823/23,824` 带 `INSUFFICIENT_PSF_SUPPORT`，`range_anomaly` 的 `36/44` 带 `NEGATIVE_OVERFLOW`；`compact_quality` 的 `26,483/26,483` 无旗标。旗标可以重叠，比例不是互斥概率；当前 `82931=CODE_PATTERN`、`82934=UNRESOLVED_BLEND`，说明双框在细筛层进入不同机制分支。实现为 `src/rst19/feature_flag_profile.py`，对应真实性研究 10.89、检测器实验记录 8.107。

82. 结果版本血缘校验：当前主表 `tmp/source-quality-audit-code-pattern-current-v3/source_catalog.csv` 为 `84,594` 行、`29,260` 个质量通过源，八类计数之和严格等于总行数，质量数之和严格等于质量通过数；旧 GUI 默认表仍有 `26,494/902/36` 等历史分母。核心短稿已锁定 current v3，旧表只作历史留档，不与 `82931/82934` 的当前 pair 证据拼接。该项校验只读，不改变检测器、GUI、质量层或缓存，用于防止每个数字各自真实但组合成不存在的实验结果；依据为检测器实验记录 8.108 与核心短稿第 1 节。
83. 旗标交互审计：新增 `rst19-feature-flag-interaction`，从当前 v3 `source_catalog.csv` 统计旗标 presence、完整集合 `exact_set`、质量通过和高 SNR 落选。`PARTIAL_MASKED` presence 为 `11,490`、其中单独出现 `2,777` 行且全部通过；`LOW_FLUX_SNR+PARTIAL_MASKED` 为 `7,703` 行、`INSUFFICIENT_PSF_SUPPORT+PARTIAL_MASKED` 为 `5,084` 行，二者质量通过均为 `0`。该结果说明质量规则存在条件交互，但旗标可重叠且与质量层部分同源，不能写成真星概率或伪影率；实现为 `src/rst19/feature_flag_interaction.py`，测试为 `tests/test_feature_flag_interaction.py`，产物为 `tmp/feature-flag-interaction-code-pattern-current-v1/`，对应真实性研究 10.91。
84. 质量门有符号余量审计：新增 `rst19-feature-gate-margin`，读取当前 v3 `source_catalog.csv`，按 8 个互斥特征类别统计 `flux_snr`、PSF 支撑、FWHM、椭圆率、sharpness 和足迹相对于当前质量门的逐字段有符号余量。弱/背景类 `flux_snr` 越界为 `21,250/21,250`，尖峰/支持不足类 PSF 支撑越界为 `23,823/23,824`；但线状类 `flux_snr` 中位余量 `+828.9`、范围异常类 `+144.3`、拥挤类 `+4.020`，说明高显著性落选可由结构/值域/去混叠规则造成，不能统一称为噪点。负余量不是噪点证明，正余量也不是恒星证明；实现为 `src/rst19/feature_gate_margin.py`，测试为 `tests/test_feature_gate_margin.py`，产物为 `tmp/feature-gate-margin-code-pattern-current-v1/`，对应真实性研究 10.92 与检测器实验记录 8.109。
85. 逐候选质量门路径审计：新增 `rst19-feature-gate-route`，对当前 v3 逐行拆分数值门越界和非数值/结构旗标，并按类别汇总 `numeric_only/structural_only/numeric_and_structural`。截图 `82931/82934` 均为 `rejected_structural_only`，分别是 `CODE_PATTERN` 与 `UNRESOLVED_BLEND`；线状类 `90/96`、范围异常类 `33/44`、拥挤类 `387/905` 的拒绝候选也属于结构型路径，而弱/背景类 `20,987/21,250` 主要属于数值型路径。该工具是观测规则分解，不是逐项关闭规则后的反事实消融；旗标和质量层可能同源，不能写成噪点率、物理恒星比例或因果贡献。实现为 `src/rst19/feature_gate_route.py`，测试为 `tests/test_feature_gate_route.py`，产物为 `tmp/feature-gate-route-code-pattern-current-v1/`，对应真实性研究 10.93 与检测器实验记录 8.110。
86. 质量门路径—跨帧响应交叉审计：新增 `rst19-feature-gate-temporal-route`，把路径表、非紧凑诊断源时序表和当前 v4 的 15 帧元数据按 `detection_id` 连接。实验口径为 `1 px` 注册关联、互相最近一对一和 `12/15` 持久线；全路径结果为 `rejected_structural_only` `776` 条中候选持久 `485`、邻域质量持久 `25`，`rejected_numeric_only` `41,592` 条中为 `31,294/491`，混合型 `12,966` 条中为 `9,388/61`。`82931` 为候选/同机制/邻域质量 `4/15、4/15、0/15`，`82934` 为 `4/15、3/15、0/15`。该结果支持“候选重复不等于质量身份”，但不提供噪点概率、precision、完备率或物理恒星数；空白时序字段表示无逐源诊断行而非零响应。实现为 `src/rst19/feature_gate_temporal_route.py`，测试为 `tests/test_feature_gate_temporal_route.py`，产物为 `tmp/feature-gate-temporal-route-code-pattern-current-v1/`，对应真实性研究 10.94 与检测器实验记录 8.111。
87. 诊断子组—规则路径—时序交叉：在同一工具中增加 `feature_gate_temporal_subgroup_summary.csv`，把 `masked_partial`、`blend_unresolved`、`weak_low_flux_snr`、`shape_elongated` 等子组的路径组成、候选持久、同机制持久和邻域质量持久并列。关键结果为 `masked_partial` 候选 `9,048/11,289` 但同机制仅 `9/11,289`，`blend_unresolved` 为 `509/905` 与 `196/905`，`weak_low_flux_snr` 为 `16,003/20,987` 与 `2,723/20,987`；这支持“位置重复与机制重复是两维证据”，但仍不是噪点率、恒星概率或物理类别真值。新表的 14 个子组计数与原始诊断子组持久表逐项一致。实现/回归仍为 `src/rst19/feature_gate_temporal_route.py` / `tests/test_feature_gate_temporal_route.py`，新增产物为 `tmp/feature-gate-temporal-route-code-pattern-current-v1/feature_gate_temporal_subgroup_summary.csv`，对应真实性研究 10.95 与检测器实验记录 8.112。
88. 指定目标的子组条件分布：同一工具支持 `--target-id` 并写出 `feature_gate_temporal_targets.csv`，以含并列值的经验 `le/ge` 比例描述目标在所属子组中的位置。`82934` 在 `blend_unresolved` 的 `n=905` 子组中，候选 `4/15` 的 `le/ge` 为 `9.4%/94.7%`，同机制 `3/15` 的 `le/ge` 为 `22.4%/85.4%`；`82931` 的 `range_code_pattern` 只有 `n=12`，只作局部定位。目标排名不是噪点概率、显著性检验或星表身份；实现仍为 `src/rst19/feature_gate_temporal_route.py`，对应真实性研究 10.96 与检测器实验记录 8.113。
89. 几何条件化近邻机制背景与注入 baseline 复核：在当前源表中筛选质心距 `≤3.5 px`、峰距 `3.5--4.8 px` 的候选对，共 `11` 对（同类别 `7`、跨类别 `4`），双质量通过 `0/11`；`82931/82934` 是其中唯一的 `range_anomaly + crowded_blend` 组合，应作为跨机制 hard negative 单独复核，不能当作所有近邻双星的代表。窗口敏感性显示质心上限放宽到 `4.0 px` 会混入 `840` 对、双质量通过 `149`，因此 `0/11` 只作目标几何的局部背景；独立 CSV/JSON 敏感性产物同时记录 `3.0/3.5/4.0 px` 与两组峰距窗口。严格窗口 `11/11` 对两端均含 Gaussian 提议，目标 pair 为 `gaussian|dog_narrow` 与 `gaussian`，所以第二框不是当前混合运行中的 DoG-only 提议；但提议器共享同一原图，不能视为独立投票。目标位置四个污染注入条件中 baseline 双候选 `4/4`、注入后 `4/4`、新增双候选 `0/4`，质量双通过 `0/4→2/4`。这把“相同几何背景下的机制一致性”和“目标位置加信号后的门控敏感性”分开，避免把注入后的 pair 命中当成原始双框成因或物理真值。实现为 `src/rst19/pair_mechanism_context.py`、`src/rst19/pair_mechanism_sensitivity.py`，测试为 `tests/test_pair_mechanism_context.py`、`tests/test_pair_mechanism_sensitivity.py`，产物为 `tmp/pair-mechanism-context-code-pattern-current-v1/` 与 `tmp/pair-mechanism-sensitivity-code-pattern-current-v1/`，对应真实性研究 10.97 与检测器实验记录 8.114。
90. 新增“父源组—子候选”解释层：不再把 NMS 局部峰直接当作物理对象，先用测量质心和局部 PSF 半径建立近邻组；只有双/多 PSF 独立分量、值域、有效像素、一对一分配和跨帧位置证据同时成立，才允许组内拆分为多个可计数候选。新增 `src/rst19/source_group_audit.py`、`source_group_audit_cli.py` 和 `rst19-source-group-audit`，只读当前 v3 `source_catalog.csv`。组半径 `3 px` 的真实运行将 `84,594` 个候选组织为 `84,586` 个组，其中 `8` 个多成员组全部为 `unresolved_group`，`0` 个达到独立候选线；`82931/82934` 为一个两成员未分辨组，`2.738 px < 3 px`。该层保留两个审计子候选，但不能在已确认恒星数中计为一颗或两颗，也不把组代表改写成恒星；实现不修改检测缓存、默认质量层或物理真值口径，回归为 `tests/test_source_group_audit.py`，产物为 `tmp/source-group-audit-code-pattern-current-v1/`，核心短稿和真实性研究分别补入第 2.2 节与 10.32.1。
91. 父源组半径敏感性控制：新增 `src/rst19/source_group_sensitivity.py`、`source_group_sensitivity_cli.py` 和 `rst19-source-group-sensitivity`，在同一 v3 源表扫描 `2.5/3.0/3.5/4.0 px`。全图多成员组数为 `4/8/12/831`，独立候选组为 `0/0/0/25`；目标在 `2.5 px` 为两个孤立候选，在 `3.0 px` 及以上均为同一 `unresolved_group`。该结果把 `3 px` 限定为 detector-level 复核半径，不是光学分辨率或物理星数；产物为 `tmp/source-group-radius-sensitivity-code-pattern-current-v1/`，回归为 `tests/test_source_group_sensitivity.py`。
92. 源组过度合并边界复核：对 `4 px` 的传递连通分组单独导出明细，`831` 个多成员组中 `821` 个为二元组、`10` 个为三元组，共涉及 `1,672` 个候选；目标仍是 `82931|82934` 的两成员 `unresolved_group`。这说明半径放大后的全图变化主要来自候选密度和连锁边界，不能解释为物理源数增加；后续 GUI/论文应把 `4 px` 作为压力测试而非默认分辨率，并保留直接近邻与父组两个层次。产物为 `tmp/source-group-audit-code-pattern-current-r4-v1/source_groups.csv`，只读源表、没有重新读取 FITS。
93. 直接近邻边对照：新增 `src/rst19/direct_neighbor_audit.py`、`direct_neighbor_audit_cli.py` 和 `rst19-direct-neighbor-audit`，在同一源表和半径下保留直接边，不做传递合并。`3 px` 下为 `8` 条边且全部 `unresolved_pair`；`4 px` 下为 `841` 条边，其中 `816` 条未分辨、`25` 条工程候选；目标 `82931|82934` 两种半径下均为同一条未分辨直接边。该层用于区分局部 pair 与父组连锁，不输出物理双星概率或星数；回归为 `tests/test_direct_neighbor_audit.py`，产物为 `tmp/direct-neighbor-audit-code-pattern-current-r3-v1/` 和 `tmp/direct-neighbor-audit-code-pattern-current-r4-v1/`。
94. 首帧符号反相对照的当前代码复跑：在已提交的 `hybrid + 4σ + FWHM=2 px + min_distance=4 px + flux SNR≥5 + PSF support≥3` 参数下重新运行 `rst19-signed-null-audit`，得到正向/反相候选与质量 `84,594/29,260`、`2,309/1`；反相唯一质量源来自原图极端负码的值域转移，排除后为 `0`。这次复跑与条目 36 的数值一致，说明核心短稿 v3.1 引入的负控结论可由当前提交复现；该对照仍只约束近似符号对称的噪声尾部，不是 FDR、p 值、precision 或物理伪影率。机器产物为 `tmp/signed-null-audit-code-pattern-current-v2/`，实现为 `src/rst19/signed_null.py` 和 `src/rst19/signed_null_cli.py`。
95. 截图 pair 的局部 Gaussian 响应反事实归因：新增 `src/rst19/pair_response_attribution.py` 与 CLI，在共同背景 `B=37 ADU`、`FWHM=2 px` 下比较 `raw`、重复码屏蔽、`≤−1000 ADU` 负异常屏蔽和联合屏蔽。raw 的主/副响应为 `3579.261/888.725 ADU`；屏蔽重复码后主位置无效，屏蔽负异常后副位置不再是局部响应极大值。`4σ` 离散核归因中，主响应的普通/重复码/负异常贡献为 `+2729.889/+1308.807/−459.535 ADU`，副响应为 `+1358.338/+0.020/−469.635 ADU`。该结果把两个框具体解释为“重复码正值锚点 + 共享亮斑翼部响应”，以及负异常在亮斑内部形成的响应凹槽；它是 detector-level 线性归因，不是噪点概率、物理星数或伪影率。机器产物为 `tmp/pair-response-attribution-code-pattern-current-v1/`，测试为 `tests/test_pair_response_attribution.py`。

对应机器产物：

- `tmp/pair-flux-ratio-multilevel-gui-default/pair_flux_ratio_audit.csv`
- `tmp/pair-flux-ratio-512-empirical-gui-default/pair_flux_ratio_audit.csv`
- `tmp/pair-flux-ratio-512-empirical-matched25-gui-default/pair_flux_ratio_audit.csv`
- `tmp/pair-flux-ratio-512-gaussian25-gui-default/pair_flux_ratio_audit.csv`
- `tmp/pair-integrated-1867-gaussian-gui-default-t2-n8/pair_flux_ratio_audit.csv`
- `tmp/pair-integrated-1867-empirical-gui-default-t2-n8/pair_flux_ratio_audit.csv`
- `tmp/source-feature-morphology-gui-default/source_feature_morphology.csv`
- `tmp/source-feature-morphology-gui-default/source_feature_psf_similarity.csv`
- `tmp/source-feature-psf-grid2-per16-code-pattern-current-v4/source_feature_psf_spatial.csv`
- `tmp/source-feature-psf-grid2-per16-gui-default/source_feature_psf_spatial.csv`（门控前历史对照）
- `tmp/crowded-blend-audit-integrated-gui-default/crowded_blend_audit.csv`
- `tmp/crowded-blend-audit-integrated1024-gui-default/crowded_blend_audit.csv`
- `tmp/crowded-blend-audit-integrated-t2-g8-gui-default/crowded_blend_audit.csv`
- `tmp/crowded-blend-audit-integrated-empirical-t1-g4-gui-default/crowded_blend_audit.csv`
- `tmp/sequence-feature-persistence-gui-default/sequence_feature_persistence.csv`
- `tmp/sequence-feature-persistence-code-pattern-current-v3/sequence_feature_persistence.csv`
- `tmp/sequence-feature-persistence-code-pattern-current-v3/sequence_feature_method_frame_summary.csv`
- `tmp/sequence-feature-persistence-code-pattern-current-v3/sequence_feature_method_profile.csv`
- `tmp/sequence-feature-persistence-code-pattern-current-v4/sequence_feature_source_subgroup_persistence.csv`
- `tmp/sequence-feature-persistence-diagnostic-subgroups-v5/sequence_feature_diagnostic_subgroup_persistence.csv`
- `tmp/feature-cross-audit-code-pattern-current-v3/feature_cross_audit.csv`
- `tmp/local-multipsf-audit-screenshot/local_multipsf_audit.csv`
- `tmp/local-multipsf-audit-screenshot/local_multipsf_audit.json`
- `tmp/spatial-injection-code-pattern-current-2x2-gaussian-t1/spatial_injection_audit.csv/json`
- `tmp/spatial-injection-code-pattern-current-2x2-gaussian-t1-high/spatial_injection_audit.csv/json`
- `tmp/spatial-injection-code-pattern-current-2x2-gaussian-t1-high-paired-v2/spatial_injection_audit.csv/json`
- `tmp/aperture-sensitivity-code-pattern-current-v1/aperture_sensitivity_audit.csv/json`
- `tmp/local-psf-availability-pair-82931-82934-current-v1/local_psf_availability.csv/json`
- `tmp/empirical-pair-fit-local-wide-1024-pair-82931-82934-current-v1/empirical_pair_fit.csv`
- `tmp/empirical-pair-fit-local-wide-1024-pair-82931-82934-current-v1/empirical_pair_fit.json`
- `tmp/empirical-pair-fit-local-wide-1024-pair-82931-82934-current-v1/empirical_pair_fit_bic.png`
- `tmp/feature-class-persistence-psf-decoupling-current-v1/feature_class_persistence_psf_decoupling.csv/json`
- `tmp/feature-parameter-sensitivity-code-pattern-current-v1/feature_parameter_runs.csv`
- `tmp/feature-parameter-sensitivity-code-pattern-current-v1/feature_parameter_profiles.csv`
- `tmp/feature-parameter-sensitivity-code-pattern-current-v1/feature_parameter_transitions.csv`
- `tmp/feature-parameter-sensitivity-code-pattern-current-v1/feature_parameter_sensitivity.json`
- `tmp/sequence-feature-parameter-comparison-current-v1/sequence_feature_parameter_comparison.json`
- `tmp/sequence-feature-parameter-comparison-current-v1/sequence_feature_parameter_runs.csv`
- `tmp/sequence-feature-parameter-comparison-current-v1/sequence_feature_parameter_profiles.csv`
- `tmp/sequence-feature-parameter-comparison-current-v1/sequence_feature_parameter_transitions.csv`
- `tmp/feature-class-context-pair-82931-82934-current-v1/feature_class_context.json`
- `tmp/feature-class-context-pair-82931-82934-current-v1/feature_class_context_targets.csv`
- `tmp/feature-class-context-pair-82931-82934-current-v1/feature_class_context_metrics.csv`
- `tmp/feature-hard-negative-code-pattern-current-v1/feature_hard_negative.json`
- `tmp/feature-hard-negative-code-pattern-current-v1/feature_hard_negative_summary.csv`
- `tmp/feature-hard-negative-code-pattern-current-v1/feature_hard_negative_rows.csv`
- `tmp/feature-hard-negative-forced-stability-current-v1/forced_stability_frame_metrics.csv`
- `tmp/feature-hard-negative-forced-stability-current-v1/forced_stability_summary.csv`
- `tmp/feature-hard-negative-forced-stability-current-v1/forced_stability.json`
- `tmp/feature-spatial-context-code-pattern-current-v1/feature_spatial_context_summary.csv`
- `tmp/feature-spatial-context-code-pattern-current-v1/feature_spatial_context_hotspots.csv`
- `tmp/feature-spatial-context-code-pattern-current-v1/feature_spatial_context_targets.csv`
- `tmp/feature-spatial-context-code-pattern-current-v1/feature_spatial_context.json`
- `tmp/temporal-code-audit-pair-82931-82934-current-v2/temporal_code_focus_series.csv`
- `tmp/temporal-code-audit-pair-82931-82934-current-v2/temporal_code_audit.json`
- `tmp/temporal-code-audit-pair-82931-82934-current-v2/temporal_code_components.csv`
- `tmp/local-multipsf-audit-screenshot/local_multipsf_bic.png`
- `tmp/local-free-multipsf-audit-screenshot/local_free_multipsf_audit.csv`
- `tmp/local-free-multipsf-audit-screenshot/local_free_multipsf_audit.json`
- `tmp/local-free-multipsf-audit-screenshot/local_free_multipsf_bic.png`
- `tmp/local-free-multipsf-radius-sweep-screenshot/local_free_multipsf_radius_sweep.csv`
- `tmp/local-free-multipsf-radius-sweep-screenshot/local_free_multipsf_radius_sweep.json`
- `tmp/local-free-multipsf-radius-sweep-screenshot/local_free_multipsf_radius_sweep.png`
- `tmp/local-multipsf-audit-screenshot-pair2/local_multipsf_audit.csv`
- `tmp/local-multipsf-audit-screenshot-pair2/local_multipsf_audit.json`
- `tmp/local-multipsf-audit-screenshot-pair2/local_multipsf_bic.png`
- `tmp/local-multipsf-audit-screenshot-pair2-range-sentinel/local_multipsf_audit.csv`
- `tmp/local-multipsf-audit-screenshot-pair2-range-sentinel/local_multipsf_audit.json`
- `tmp/local-free-multipsf-radius-sweep-screenshot-pair2/local_free_multipsf_radius_sweep.csv`
- `tmp/local-free-multipsf-radius-sweep-screenshot-pair2/local_free_multipsf_radius_sweep.json`
- `tmp/local-free-multipsf-radius-sweep-screenshot-pair2/local_free_multipsf_radius_sweep.png`
- `tmp/local-multipsf-fwhm-sweep-pair2-f1.5/` 至 `tmp/local-multipsf-fwhm-sweep-pair2-f3.5/`
- `tmp/source-quality-audit-code-pattern-current/source_quality_summary.csv`
- `tmp/source-quality-audit-code-pattern-current/source_catalog.csv`
- `tmp/source-quality-audit-code-pattern-current/source_feature_subclass_summary.csv`
- `tmp/sequence-feature-persistence-code-pattern-current/sequence_feature_persistence.csv`
- `tmp/sequence-feature-persistence-code-pattern-current/sequence_feature_temporal_profile.csv`
- `tmp/sequence-feature-persistence-code-pattern-current/sequence_feature_class_transition.csv`
- `tmp/feature-cross-audit-code-pattern-current/feature_cross_audit.csv`

## 正文采用的判定层级

```text
宽筛候选响应
  -> 原始像素质量审计
  -> PSF/形态与异常值域诊断
  -> 跨帧注册坐标响应
  -> WCS/星表唯一匹配与分层注入校准
```

其中 `quality_passed` 只表示当前图像质量规则通过，`12/15` 只表示注册坐标邻域响应持久，`pair_resolution_recall` 只表示已知注入双源是否被两个不同候选一对一恢复。只有在 WCS、星表、设备值域和阴性样本得到验证后，才可以把这些中间量提升为物理身份或正式完备率。

## 未决事项

- FITS signedness、`BLANK`、坏像素和满阱定义；
- 增益、读出噪声和完整 Poisson 误差；
- detector 分区留出 PSF 与更多 trial；当前空间 PSF 类别诊断仍是 `2×2/16` 的候选抽样，多源拥挤控制虽已扩展为每条件 `16` 组，但仍需更多方向/位置，等积分双源对照仍只有 `2×8` 对/条件；
- 真实运动目标逐帧真值和完整 WCS；
- 星表版本、滤波系统、颜色项和唯一匹配残差。
- 当前固定位置 `K` 模型仍假设全局 Gaussian PSF、已知中心和共享背景；需用分区留出 PSF、自由位置多源拟合和官方星表/WCS 复核后，才能决定该局部是否包含多个物理源。
- 当前自由位置模型只在初始候选周围做有限半径优化；F07 的 `1/15` 结果随半径变化，仍需多起点、分区 PSF、完整噪声似然和注册天空坐标复核。
- `CODE_PATTERN` 是当前 FITS 的可解释数据质量门，不是主办方已确认的坏像素/饱和定义；需取得 signedness、`BLANK`、ADC 满阱、增益、读出噪声和坏像素资料后再校准。
