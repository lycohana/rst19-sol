# 创新交付审计

状态：**条件可答辩，尚未完成最终科学复核**

本文件是对当前仓库 HEAD 的创新交付审计。它把“可以作为比赛创新结果表述的量”和“只能用于算法诊断的量”分开，避免把检测器内部统计、可视化图或背景检测数夸大成物理真值。

## 1. 选择的主创新

最终优先交付：**真实背景分层注入—回收的条件化可探测性剖面**。

它的可答辩表述是：在真实 FITS 图像背景中，按候选稀疏/低纹理、高背景、边缘等条件选取位置，注入位置和名义强度已知的 PSF 源；然后分别统计宽松候选层和严格质量层对这些已知源的回收率。

它测量的是：

```text
召回率 = 回收的已知注入源数 / 无歧义已知注入源数
质量筛选损失 = 候选层召回率 - 质量层召回率
```

这组结果能说明当前真实背景和当前检测参数下的**条件化经验可探测性**。它不等于整幅图真实恒星的普适完备率，也不等于 precision、误检率、FDR、物理仪器灵敏度或绝对星等极限。

## 2. 当前仓库中的证据分级

### 主创新：可以进入创新赛项材料

当前分层注入实验 `tmp/stratified-real-background-code-pattern-current-n8/stratified_real_background_injection.json` 有 18 个条件/强度单元，6 种背景条件，3 个名义峰值层（24/56/128 ADU）。其中 `blank`、`high_background`、`edge` 三个条件没有歧义注入位置，每个条件/强度有 8 个无歧义注入位置，因此可以组成 3 条共享强度的条件化剖面。

这三条剖面在当前数据中观察到：

| 条件 | 24 ADU 候选/质量召回 | 56 ADU 候选/质量召回 | 128 ADU 候选/质量召回 | 局部噪声约值 |
| --- | ---: | ---: | ---: | ---: |
| blank | 0/0 | 1/0 | 1/0.625 | 5.56 ADU |
| high_background | 0.25/0 | 0.875/0 | 1/0.5 | 10.01 ADU |
| edge | 0/0 | 0.875/0 | 1/0 | 7.60 ADU |

上表是描述性结果，不做显著性检验。每格的 `n=8` 是一个实验布局内的注入位置数，不是 8 次独立重复实验；Wilson 95% 区间会在交付 CSV 中同时给出，以展示小样本的不确定性。

主结果可以谨慎概括为：候选层和质量层之间存在明显筛选损失，而且损失依赖背景条件和注入强度。这个结论比“检测到了多少颗星”更可复核，因为每个主剖面单元都有已知的注入分母。

### 困难条件：有价值，但只能作为控制/边界

`crowded`、`special_code`、`line` 被保留在输出中，但不纳入主剖面。原因不是它们“没有信息”，而是注入位置存在邻源、特殊值或长线污染，导致“注入源是否被正确回收”的归因不再干净。它们适合回答：算法在哪些局部条件下失效或需要单独分支；不适合和空白条件混合成一个总召回率。

### 基础/支撑证据：不能单独当创新

- 15 帧运动、配准和差分：支撑基础赛项的动目标识别，并为实验背景提供时间关系；没有 WCS 时只能报告像素平面位移、速度和拟合残差。
- 注册中值拼图：可以直观展示配准覆盖和固定星场增强，但不是新的星点计数、光度定标或轨道结果。
- `innovation.py` 综合报告：是证据导出器，不会自动完成创新指标的真值核验和交付分级。

### 诊断工具：不能直接写成“发现了恒星概率/误检率”

特征证据矩阵把值域、形态、PSF 留一法和空间信息组合成路由指标，适合定位 `weak_or_background`、`crowded_blend`、`spike_or_support` 等失败模式。但它没有官方逐星真值或独立负样本；候选持续性、PSF 相关度、路由比例都不能直接解释为恒星概率、precision 或 FDR。

## 3. 审计实现与复核命令

新增 `innovation_package.py` 只读取已有 JSON/CSV，完成以下审计：

1. 校验计数边界、召回率范围和 `(stratum, peak_excess_adu)` 网格唯一性；
2. 只把无歧义注入位置纳入主创新分母；
3. 计算候选/质量召回率、质量筛选损失和 Wilson 95% 区间；
4. 把困难条件、15 帧报告和特征矩阵分级保存；
5. 记录输入文件 SHA-256、行数、条件和强度层，保证结果可追溯；
6. 不读取 FITS，不重新检测，不把背景检测数解释成误检数。

复核命令：

```powershell
python -m pytest -q tests/test_innovation_package.py

python -m rst19.innovation_package_cli `
  tmp\stratified-real-background-code-pattern-current-n8\stratified_real_background_injection.json `
  --out-dir $env:TEMP\rst19-innovation-package `
  --sequence-report tmp\innovation-current-psf-support\innovation_report.json `
  --feature-matrix tmp\feature-evidence-matrix-code-pattern-current-v3-routing\feature_evidence_matrix.json `
  --require-defensible
```

输出为 `innovation_package.json`、`innovation_delivery.csv` 和 `innovation_delivery.md`。建议输出到临时目录或比赛附件目录；不要把 FITS、`tmp/` 结果或 cache 加入 Git。

## 4. 答辩时可以说什么

> 我们没有把全图检测数直接称为恒星数，而是在真实 FITS 背景中注入已知源，按局部背景条件分层，分别统计候选层和质量层的回收率。结果表明，弱源是否进入严格质量层不仅与名义强度有关，也与局部背景条件有关。因此我们交付的是当前数据和当前参数下的条件化经验可探测性剖面。由于每格只有 8 个注入位置且尚无官方逐星真值，我们不把它外推为普适完备率、误检率或物理灵敏度。

建议展示三样东西：

1. 一张三条件双曲线图（候选召回和质量召回用不同线型）；
2. 一张含 `n` 和 Wilson 区间的表；
3. 一张困难条件对照图，并明确它是边界/失败模式，不混入主曲线。

## 5. 未完成项

- 每个条件/强度至少做 3 个独立位置布局，并扩大空间采样；当前 `trial_count=1`。
- 获取官方逐星真值或构造独立负样本，才能审计 precision/FDR 和真实恒星完备率。
- 补齐真实相机的增益、暗电流、平场、非线性、波段响应和标准星零点，才能把 `m_inst` 推进到可辩护的绝对星等。
- 使用留出星表匹配和空间变 PSF 对照，验证剖面能否跨区域/跨帧迁移；当前注入源仍是实验模型源。
- 对照公开论文和同类作品后再陈述创新性；本审计只判断数据证据和交付边界，不自动宣称领域独创。

## 6. 最终交付清单

- [x] 主创新定义、计算公式、分母和限制。
- [x] 真实背景分层注入结果的条件化剖面。
- [x] 候选层/质量层分开统计，并保留 Wilson 区间。
- [x] 困难条件作为控制证据，不混入主结论。
- [x] 15 帧报告与特征矩阵的角色分级。
- [ ] 独立重复布局至少 3 次。
- [ ] 官方逐星真值/独立负样本。
- [ ] 绝对光度定标与最终论文数据冻结。
