# 本地数据核验记录

状态：**已核验基础信息；统计和算法结果待补充**
核验日期：2026-08-27

数据位置：`doc/00-项目资料/原始数据/`

## 1. 文件级结果

| 项目 | 结果 |
| --- | ---: |
| FITS 文件数量 | 15 |
| 单文件大小 | 33,557,312 字节 |
| 文件总大小 | 503,359,680 字节 |
| 文件大小是否一致 | 是 |
| 文件扩展名 | `.fits` |

文件名按时间顺序为：

```text
20260330163205413_9901.fits
20260330163206886_9901.fits
20260330163208415_9901.fits
20260330163209888_9901.fits
20260330163211417_9901.fits
20260330163212890_9901.fits
20260330163214419_9901.fits
20260330163215892_9901.fits
20260330163217421_9901.fits
20260330163218894_9901.fits
20260330163220423_9901.fits
20260330163221896_9901.fits
20260330163223425_9901.fits
20260330163224898_9901.fits
20260330163226427_9901.fits
```

## 2. FITS 头核验

15 个文件的核心字段均一致：

| 字段 | 当前值 |
| --- | --- |
| `SIMPLE` | `T` |
| `BITPIX` | `16` |
| `NAXIS` | `2` |
| `NAXIS1` | `4096` |
| `NAXIS2` | `4096` |
| `SITELONG` | `0.00000` |
| `SITELATI` | `0.00000` |
| `SITEALTI` | `0.00` |
| `AZIMUTH` | `270.00000` |
| `ELEVATIO` | `0.00000` |
| `EXPOSURE` | `1500 ms` |
| `DATE-OBS` | 每个文件不同，构成时间序列 |

以图像数据从偏移 2880 字节开始计算：

```text
2880 + 4096 × 4096 × 2 = 33,557,312 字节
```

这个结果与每个文件的实际大小一致，支持当前文件为单个 4096×4096、16 bit 图像的判断。

## 3. 首行辅助数据核验

比赛方说明指出，图像数据第一行的前 208 字节保留为辅助数据，即第一行前 104 个 16 bit 像素位置不应直接当作普通图像像素。按说明中的 16 位字节交换后，以小端格式解包 26 个 `double`，当前首个文件得到：

- `p_az = 270.0`
- `p_el = 0.0`
- `ra ≈ 129.533548`
- `dec ≈ -1.845372`
- `q1..q4 ≈ (0.658783, 0.224458, -0.671886, 0.253363)`
- `roll/pitch/yaw ≈ (59.4144, -14.9257, 9.4498) deg`

其中 `p_az`、`p_el` 与 FITS 头一致，且姿态四元数数值量级合理；这只是首个文件的解码抽检，正式实现仍需对 15 个文件逐一检查字段有限性、四元数范数、位置/速度连续性和跨语言结果。

## 4. 图像值抽检

对首个文件按 FITS 16 bit 大端存储读取，并暂时排除首行前 104 个辅助数据位置后，得到：

- 数据数组形状：`(4096, 4096)`，对应 FITS 的 `(NAXIS2, NAXIS1)`。
- 数据类型：`>i2`，即大端 16 bit 有符号整数。
- 像素最小值：`-32726`。
- 像素最大值：`32750`。
- 非零像素约 16.70M 个；存在少量负值和大量低值背景像素。
- 99.9 分位约为 `1232.8`，99.99 分位约为 `7776.6`。

这些数字只用于确认数据范围和后续参数初始化，不能直接解释为物理亮度或星等。负值、接近 16 bit 极值的值、首行辅助区域和可能的饱和点都需要在算法中显式标记。

## 5. 已知冲突与处理决定

原始 DOCX 正文写有“4096×4098×16bit”，但实际 15 个文件的 `NAXIS2` 均为 4096，且文件大小严格符合 4096×4096×16 bit 加 2880 字节头。当前处理决定：

1. 以每个 FITS 文件的合法头字段为准。
2. 解析前检查 `NAXIS1`、`NAXIS2`、`BITPIX` 与文件长度。
3. 把说明文档中的 4098 记录为待向比赛方确认的文字差异，不在代码中写死 4098。

## 6. 待补充核验

- 是否存在 `BSCALE`、`BZERO` 等缩放字段，以及读取后应使用存储值还是物理值。
- 是否有暗场、平场、坏点图、饱和阈值或图像裁剪规则。
- 15 帧之间的精确时间间隔、姿态变化和静态星点位移统计。
- 第一行辅助数据是否只占用前 208 字节，还是同时影响 FITS 图像坐标解释。
- 是否能从 `ra`、`dec`、姿态四元数和相机内参恢复完整 WCS。
