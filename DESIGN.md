# rst19-sol 项目设计

## 1. 项目目标

为开运一号天基图像比赛建立一套可复核的资料、算法和实验仓库，先完成 FITS 数据边界与星点检测基线，再逐步扩展到星表匹配、星等标定、运动目标识别和创新分析。

## 2. 当前边界

- 原始 FITS 数据保留在本地 `doc/00-项目资料/原始数据/`，不进入公开 Git 历史。
- `src/rst19` 当前包含单帧离线检测、局部测光、切平面 WCS/先验星表匹配、15 帧平移关联、创新分析证据导出和本地 Tkinter 工作台。
- FITS 辅助字段已经可以由四元数和相机机体系 `-Y` 轴恢复逐帧光轴 RA/Dec；这是一条已用 15 帧真实数据核验的姿态先验路径，不等于完整 WCS。
- 任何检测数量、星等或运动目标结论都必须经过真实数据参数扫描、误差分析和人工/合成验证后才能写成正式结果。

## 3. UI 设计读取：qiaomu-design 适配

本项目的设计读取为：面向参赛队伍、指导老师和答辩演示的本地科学工作台，使用“观测站实验台 / field notebook”语言，优先让用户在 3 秒内看到当前帧、运行状态和证据边界。这里采用 [qiaomu-design](https://github.com/joeseesun/qiaomu-design) 的反模板原则和打磨模式，并将其适配为 Python/Tkinter 的桌面界面；qiaomu-design 本身是设计工作流，不是本项目的 Web 运行时依赖。

本项目固定的三拨盘为：视觉冒险度 `4/10`、动效强度 `3/10`、信息密度 `7/10`。因此界面采用纸张色、深靛蓝、琥珀和青绿色，保留非对称的主图/证据栏和编号式数据表，但不使用紫色渐变、玻璃拟态、Inter、居中营销 Hero 或无法解释的装饰数字。核心功能契约是：选择 FITS、运行单图/15 帧分析、看见进度、区分候选/质量/moving、查看星等/速度/方向/预测，并能导出可复核表图；任何视觉调整都不能挤掉这些动作或把长文本顶坏布局。

已落地的打磨门禁：主工作区先显示图像和参数口径；进度同时出现在参数区、底部状态栏和右侧证据卡；长文本按容器宽度换行；结果窗口使用可滚动表格和按需图证；错误、缓存命中、旧任务丢弃和无 WCS 的边界都有明确状态。后续 UI 变更必须继续通过这一组 Audit / Polish / Harden 检查，不引入 Web 页面或把科学数据包装成仪表盘装饰。

## 3. 目录关系

```text
doc/                 规则、数据核验、方法研究和参考资料
src/rst19/           可复用 Python 实现与本地 Tkinter 桌面工作台
tests/               不依赖原始 FITS 的单元测试
pyproject.toml       安装、依赖和 CLI 入口
```

## 4. 关键决策

| 决策 | 当前选择 | 理由 |
| --- | --- | --- |
| 原始数据管理 | 本地归档，FITS 忽略 | 数据集约 480 MiB，公开和再分发授权尚未确认 |
| 算法入口 | `rst19` CLI + `rst19-gui` + Python API | 既方便离线演示，也方便测试和后续批处理 |
| 第一版匹配 | 先验 WCS 下的半径约束一对一匹配 | 先固定坐标约定和输出残差，再实现四星/五星盲解算 |
| 星表依赖 | 用户显式提供 CSV | 仓库不内置未授权星表；运行时记录路径、列定义、WCS 参数和匹配残差 |
| 星等语义 | 暂不把 Gaia G 直接命名为 `Mv` | 相机响应曲线和颜色转换尚未核验 |
| 检测数量语义 | `candidate` / `returned` / `quality` 三层 | 不把候选峰、质量源、匹配源或物理真值混成一个数字 |
| SNR 语义 | 峰值、匹配滤波、孔径通量 SNR 分开 | 最暗源使用通量 SNR，未知相机参数不虚构噪声项 |
| 序列基线 | 高质量静态源平移配准 + 互相最近的一对一轨迹关联 + 注册时间中值差分 | 先扣除成像系统位移，再以连续出现、总位移、拟合残差和时序残差判定运动；避免重复使用同一静态源 |
| 线状结构处理 | 点源层标记 `LINE_ARTIFACT`；序列层独立检测高残差线状候选并跨帧拟合 | 亮线不污染可信点源和点轨迹，同时保留为运动分析的独立证据 |
| 单像素尖峰处理 | 候选峰中心 3×3 核心至少有 3 个像素超过 `max(2σ_local, 0.1×peak_excess)` 才进入质量层 | 1σ 孔径 footprint 容易把高单像素脉冲与随机正噪声误当成点源；候选仍保留，质量层拒绝原因可审计 |
| 辅助姿态先验 | 四元数主动旋转 + 机体系 `-Y` 光轴恢复 RA/Dec，并逐帧审计 | 15 帧数据中与辅助 RA/Dec 达到亚纳角秒级一致，可约束星表搜索；缺少像元尺度、轴向矩阵和畸变时不升级为完整 WCS |

## 5. 已知限制

- 当前代码尚未提供完整 WCS 关键字解析、Astrometry.net 索引和自动盲解算。
- 当前 128 px 局部背景网格和圆 Gaussian PSF 仍是基线，不能替代实测空间变 PSF、复杂散射光模型或完整差分。
- 当前 `quality_count` 是质量定义下的可信点源数，不是未经星表、注入和人工验证的比赛真值；启用线状结构审计后的首帧结果仍不能直接作为提交结果。
- 当前 15 帧序列仍以全局平移配准为主；星表核验窗口已经提供“先验 WCS 引导的一对一匹配 → 局部仿射校准”的独立标定路径，但它不等于序列级仿射配准或全天空盲解算。GUI 已接入点轨迹、单帧长线候选、独立线状候选层和按需图证生成，`rst19-innovation` 已能导出逐帧/逐轨迹/注册中值差分证据；运动目标真值、绝对星等标定和比赛评测脚本仍未完成。
- 注入-回收实验同时提供简化 Gaussian 均匀背景、真实 FITS 相对空白背景和真实 FITS 分层背景三种口径；分层实验已支持经验 PSF，并覆盖低纹理、高局部噪声、边缘、拥挤和特殊值域邻域。真实数据仍只有首帧小样本，不能代替分区实测 PSF、官方逐星真值或正式 precision/FDR 评测。
- 多帧线状检测在当前小平移数据上采用分块时间中值参考；累计平移较大时才启用插值，当前仍不是完整的空间变 PSF matching 或 proper image subtraction。
- 辅助遥测位置字段按格式说明记为 `m`；速度字段原文漏写 `/s` 的可能性由位置—时间差分强烈支持，但在主办方确认前 UI 和报告均保留 `m/s?` 及逐帧自洽误差，不据此宣称完成轨道解算。

## 6. 变更历史

### 2026-09-02 - 核验辅助姿态与光轴先验

新增 `attitude.py`、`rst19-aux-audit` 和对应测试，对格式说明中的 26 个辅助字段执行 15 帧真实数据审计。按向量在前、标量在后的 Hamilton 四元数主动旋转，并以相机机体系 `-Y` 轴计算光轴 RA/Dec；四元数模长最大偏差为 `2.22e-16`，计算光轴与辅助 RA/Dec 的最大残差约 `1.1e-10` 角秒，`p_az/p_el` 与 FITS 头一致 `15/15`。

该结果把“无完整 WCS”细化为“有可靠的逐帧中心/姿态先验，但缺少像元尺度、完整轴向方向和畸变模型”。因此后续星表路线可以从姿态先验开始收窄候选、拟合局部 WCS，并保留留出匹配；不能把辅助 RA/Dec 直接当作每颗检测源的天球坐标或官方真值。

### 2026-08-27 - 开始实现星表路线

新增 `src/rst19` Python 包、单元测试和 CLI；同步补充根 README 的安装/运行说明。实现以现有数据核验和星表路线研究文档为边界，保留真实 FOV、相机尺度、WCS 方向和星等波段作为待实测参数。

### 2026-08-27 - 增加本地分析界面

新增 `rst19-gui` 本地 Python/Tkinter 工作台，连接 FITS 帧列表、预览生成和星点检测 pipeline；界面展示全部返回候选源，并用质量筛选后的仪器星等标出最暗可信源，不把候选数包装成正式比赛结果。

### 2026-08-28 - 增加科研检测和序列基线

检测模块增加局部背景/RMS、Gaussian PSF 匹配滤波、孔径通量误差、三种 SNR 和可解释质量标志；序列模块增加平移配准、唯一轨迹关联和 `static/moving/transient` 分类。实测对照与论文答辩底稿同步保存，未把任何外部示例数量写入参数。

### 2026-08-28 - 修正动目标显示语义并增加线状伪迹审计

GUI 增加“可信星点 / 全部候选 / 运动候选”三种叠加层和“分析 15 帧动目标”入口；运动层不绘制整幅质量星场，显示独立的洋红色 `moving` 线状轨迹、橙色 `candidate` 线状轨迹和严格 `moving` 点轨迹，并支持悬停查看线长、残差 SNR、轨迹位移、速度和拟合 RMS。单帧检测增加基于显著连通域 PCA 的长线结构标志 `LINE_ARTIFACT`；候选仍保留在审计列表，但默认不进入 `quality_sources`。序列层从原图高残差连通域建立线状候选池，使用面积/PCA 形状和连续跨帧关联排除普通静态点与孤立噪声；点轨迹改为拟合位移并禁止跨漏帧重连。序列配准改为互相最近的一对一匹配，并补充过期后台任务的缓存写入保护。

早期全量序列回归在 `4σ / min_distance=4 / PSF FWHM=3 / flux_snr≥5` 下得到 9,874 条稳定轨迹、0 条点源 `moving`、89,647 条瞬态轨迹，另有 1 条连续 7 帧的线状 `moving` 候选和 1 条第 1 帧单帧长线候选。当前快速序列另有明确的 6000 源工作集口径，点轨迹数量不能与该全量记录直接比较；线状检测仍从原图独立运行。该线状目标仍是算法候选，不是真值。旧记录中的 24,426/19,061 等数字来自不同参数/代码口径，不能与本次结果做单因素比较。缓存清理使用代际标记和写入锁，即使任务运行中清理，也不会由旧任务重新生成已清除的缓存。

### 2026-08-28 - 增加单帧长线入口和 15 帧证据导出

单帧“分析当前帧”复用序列层的高残差、有效像素、连通域和 PCA 几何筛选，将超长线显示为橙色候选；只有跨帧持续、配准坐标位移足够且拟合 RMS 合格时才标记为 `moving`。新增 `rst19-innovation`，按真实 `DATE-OBS` 输出逐帧时间关系、像素配准稳定性、辅助姿态摘要、稳健背景统计和运动轨迹证据，明确不把缺少 WCS/像元尺度的像素量解释成角秒或真实速度。

### 2026-08-28 - 增加可复现星点实验和证据页

新增固定随机种子的 Gaussian 点源注入-回收实验与 `rst19-injection` 命令，输出候选层/质量层召回率 CSV 和曲线；15 帧证据窗口增加“星点实验”页，默认不在打开窗口时计算，点击后才在后台运行并回填表格和图表。这里保留的 `9,874` 条稳定点源轨迹是 PSF 支持审计前的历史序列口径；当前版本另有 `9,938` 条严格静态、`6,249` 条低置信持续候选，不能混用。

### 2026-08-29 - 增加真实首帧阈值扫描和图证页

新增 `rst19-sweep`，在同一真实首帧上扫描候选阈值并输出候选峰、质量源、拒绝数、线状审计数和数量曲线；15 帧证据窗口增加“阈值扫描”页。图证页按需生成注册中值拼图、原始 ADU 正/负残差差分图及差分统计 CSV，所有后台结果通过 Tk 主线程回填，避免打开证据窗口时提前计算或被旧任务覆盖。

### 2026-08-29 - 增加真实 FITS 背景注入-回收与实测 PSF 模式

新增真实背景注入实验与 `rst19-injection --real-fits` 入口；在内存副本中注入 Gaussian 或从亮、孤立质量源中值叠加得到的 empirical PSF，继承 int16 基线的饱和/零值掩膜，复用同一批相对空白位置比较不同峰值档位，并输出候选/质量召回率、基线数量、背景区域检测数和净数量变化。新增 `rst19-sweep --psf-fwhms` 固定阈值扫描实测首帧的匹配滤波尺度，15 帧证据窗口的“星点实验”页提供理想背景与真实 FITS 背景按钮，“阈值扫描”页提供阈值和 PSF 两种扫描，真实实验按档位回报后台进度。

### 2026-08-29 - 增加星表核验窗口和时序残差基线

主界面新增“星表核验”入口，用户可选择自己的 CSV 星表并填写已标定的先验 WCS；窗口显示一对一匹配、检测/预测像素坐标、残差、目录星等，并可在主图切换到星表匹配叠加层。星表仍是身份核验和标定工具，不替代检测，也不把匹配数量当成恒星总数。多帧线状检测增加注册时间中值参考和差分噪声 `sqrt(2)·σ` 口径；当前 15 帧真实结果保持第 1 帧单帧候选与第 9–15 帧连续候选的分组不变。

### 2026-08-29 - 增加图像平面预测轨迹显示

运动叠加层在最后观测帧绘制独立青绿色虚线，表示基于最后两点的 `+5` 帧匀速图像平面外推；它与洋红色实测线区分，明确标为 `forecast`，不宣称为轨道预测。时间中值参考使用分块实现，减少 GUI 后台的额外浮点内存。

### 2026-08-29 - 固定历史轨迹的逐帧配准坐标

修正运动叠加层：历史轨迹中的每个点现在使用该点所属帧的累计平移恢复到当前原图坐标，末帧预测只使用末帧配准量；新增回归测试覆盖不同帧 shift，避免视场抖动较大时把整条轨迹错误套用当前帧坐标。

### 2026-08-29 - 增加辅助遥测位置—速度自洽核验

**变更内容**：对 15 个真实 FITS 按 `DATE-OBS` 做位置差分，端点使用一侧差分、中间帧使用中心差分，并把位置差分速度、速度残差和相对误差写入创新报告、逐帧 CSV 和 Tkinter 的“辅助遥测”页；保留原有 `*_raw` 字段，速度单位在界面和文档中标为 `m/s?`。

**变更理由**：格式说明明确位置为 `m`，但速度字段表格原文写成 `m`。不能凭字段名直接修正文档，也不能继续把全部向量笼统写成 raw；需要用原始 15 帧的时间序列检验两种解释是否自洽。

**影响范围**：`innovation.py` 的报告/CSV 字段、辅助遥测 UI、创新分析与本地数据核验文档；不修改 FITS 原图，不把载体状态速度与图像长线 `px/s` 混为一谈。

**决策依据**：J2000/WGS84 中间帧位置差分与辅助速度的相对误差均约为万分之几，强烈支持速度实际单位为 `m/s`、DOCX 漏写 `/s`；在主办方正式确认前，代码保留假设状态和逐帧残差证据。

### 2026-08-30 - 为图像平面轨迹增加小样本不确定度

**变更内容**：对三点及以上的跨帧线状轨迹按真实 `DATE-OBS` 分别拟合 X/Y 常速度 OLS，输出速度分量标准误、速度模长 95% 区间、方向 95% 半宽和轨迹均值外推的逐轴 95% 区间；UI 创新摘要和论文式轨迹图同步显示这些边界。两点轨迹不输出区间，单帧候选不输出速度。

**变更理由**：只有速度和预测点的点估计会掩盖 7 帧小样本、质心误差和模型假设，无法支持论文式误差分析。

**影响范围**：创新报告 schema、`motion_evidence.csv`、轨迹图、Tkinter 创新摘要和 04 创新分析文档；不改变候选检测、跨帧分类或缓存中的原始轨迹。

**决策依据**：使用残差自由度对应的 Student-t 临界值，并以 delta method 传播速度/方向误差；外推区间只描述拟合轨迹均值，不声称覆盖配准系统误差、模型失配或未来单次观测噪声。

### 2026-08-29 - 增加单图长线逐帧审计

**变更内容**：新增 `rst19-trails` 命令，逐张独立运行单帧残差/PCA 长线筛选，输出候选数量、最长线长度、宽度、方向、残差 SNR、触边状态和包围盒的 CSV 与曲线；主界面单帧分析完成后在右侧摘要显示同一组证据，15 帧证据窗口新增按需“单图长线”页，表格和曲线与命令行使用同一套参数。

**变更理由**：序列轨迹结果不能证明每一帧都存在可见长线。需要把“单图形状证据”和“跨帧运动证据”拆开，直接检查是否出现了整幅图误标或中间帧缺失。

**影响范围**：`experiments.py`、`trail_cli.py`、`pyproject.toml`、Tkinter 单帧摘要以及运动目标/创新分析文档；不改变长线判定门槛，也不把单帧候选升级为运动真值。

**决策依据**：真实 15 帧审计在当前固定参数下得到第 1 帧 1 条强长线、第 2–8 帧 0 条、第 9–15 帧各 1 条；这为连续 7 帧轨迹提供独立帧级支持，同时保留第 1 帧为不同候选组。

### 2026-08-29 - 增加集中式创新摘要页

15 帧证据窗口新增“创新摘要”页，将真实 `DATE-OBS` 时序、总时长、中位帧间隔、平移配准稳定性、线状候选的位移/速度/方向/拟合 RMS、末帧后 `+5` 帧图像平面外推坐标，以及 J2000/WGS84 辅助遥测的单位边界集中成可滚动表格；“图证”页同时显示逐帧裁剪和已生成的拟合图像平面轨迹。新增页面只整理现有 `SequenceResult`，不重新检测、不把图像 `px/s` 与载体 `m/s?` 混用；缺少完整 WCS 时明确显示不输出角秒和真实天体速度。

### 2026-08-29 - 增加源级星点研究表

新增 `rst19-sources` 和主界面“研究工具”菜单中的“导出星点研究表”，从已完成的 `DetectionResult` 导出全量源表、可重叠质量标记汇总、互斥类别摘要、按类别的形态/SNR/空间分位数与拒绝原因表、三个 SNR 口径的排名曲线和通量 SNR 分布；导出层不重新检测、不把质量数量包装成物理恒星真值。`high_flux_snr_rejected_count` 仅是高显著拒绝候选的诊断切片，不能解释为伪影真值。

### 2026-09-02 - 增加特征类别审计与分层控制

源表新增按固定优先级生成的互斥 `feature_class` 和 `source_feature_summary.csv`，同时保留原始可重叠 `flags`。新增 `rst19-injection --feature-audit`，对孤立/弱源/宽窄 PSF/双源/尖峰/掩膜/边缘/饱和/长线阴性样本分别记录已知源回收率和特征邻域泄漏；合成控制结果不能冒充真实 FITS 的伪影比例或逐星真值。

同日为源表增加 `source_feature_morphology.csv`：在首要类别内分别统计 peak/flux/filter SNR、FWHM、椭圆率、sharpness、PSF 支持、footprint、质心偏移和 X/Y 分位数，并记录范围异常、线状、掩膜/边缘、拥挤、尖峰、弱通量和形状标志的比例；另增加 `source_feature_spatial.csv`，按 4×4 粗网格记录各类别的候选/质量数和高 SNR 拒绝数。该表用于回答“高 SNR 是否仍有结构性拒绝原因”和“某类结构是否空间集中”，不改变质量层判定，也不把算法类别直接命名为物理恒星类别。

同日为有 FITS 原图的源级导出增加 `source_feature_psf_similarity.csv`：对每个首要类别抽取固定数量的原始裁剪，与干净隔离源构造的经验 PSF 比较相关系数、相对残差和中心能量占比。另增加 `source_feature_psf_spatial.csv`，以 4×4 detector 网格构造不泄漏的局部经验 PSF；局部池外仍使用整幅候选表检查邻峰，模板不足时只记录 `local_fallback_count`，不伪造局部相似度。两张表只用于检查二维轮廓、拥挤程度和类别机制，不把 `corr≥0.8` 变成新的质量阈值；首帧实测中线状类别集中在单个 4×4 网格，而拥挤候选遍布视场，局部模板可得率本身也暴露了高密度场的隔离限制，仍需逐源 PSF、星表和注入留出验证。

同日新增 `rst19-feature-sequence` 的真实 15 帧首要特征持久性审计：每帧完整返回源级测量，用质量源估计平移，在首帧 `1 px` 邻域内以互相最近的一对一响应配对分别统计候选和质量响应的 `12/15` 帧出现率。按最新 `CODE_PATTERN` 版本重跑后，紧凑质量类为 `25,028/26,376` 候选邻域、`21,543/26,376` 质量邻域达到门槛；尖峰/支持不足类为 `17,521/23,824` 候选、`87` 个质量邻域；数据有效性/范围异常类为 `33/159` 候选、`0` 个质量邻域。这进一步说明“重复出现”不是恒星充分条件。该审计保留为类别优先级证据，不替代星表天球身份匹配、注入回收和人工真值；截断工作集结果不与全量质量率混用，当前产物为 `tmp/sequence-feature-persistence-code-pattern/`。

同日新增 `sequence_feature_temporal_profile.csv`：在首帧锚点持久性之外，按每帧类别计数统计候选占比 CV、候选数量范围、质量数量范围、质量计数和活跃帧数。最新结果中紧凑/尖峰/弱通量类别的候选占比 CV 约为 `0.005/0.008/0.007`；数据有效性类别在 `15/15` 帧活跃但质量计数始终为 `0`；`compact_quality` 按 `quality_passed=True` 定义，表中的质量比例包含定义性泄漏，不是独立真阳性率。线状点源旗标平均只占 `0.013%` 且占比 CV 为 `2.125`。该表用于区分“重复的检测器结构”和“质量层通过的点源候选”，不替代星表身份、独立 PSF/注入验证或逐星真值。

同日新增 `sequence_feature_class_transition.csv`：在累计平移注册坐标中以 `1 px` 互相最近的一对一配对，同时保留首帧锚点类别和响应帧类别，并分别输出候选层、首帧质量锚点层的“任意类别响应/同类响应”。最新 `CODE_PATTERN` 版本中，实测边缘/掩膜候选的任意响应率为 `87.03%`，同类率仅 `26.84%`；线状和数据有效性/范围异常同类率为 `6.67%/46.29%`；紧凑质量锚点候选同类率为 `78.93%`，质量锚点子集同类率为 `80.45%`。这一步用于识别“邻域命中被类别切换放大”的情况，不把类别转移解释成物理天体变化，也不替代星表/WCS、PSF 留出、注入回收或逐星真值。

同日新增 `rst19-pair-audit` 的固定 detector 坐标近邻双源审计：针对两个用户指定峰位逐帧复算源级候选，并并行记录副孔径的重复码、固定 `-1`、极端正负码和原始/范围屏蔽/固定值屏蔽下的单-双 PSF `ΔBIC`。它只服务于局部异常和近邻去混叠的证据解释，不改变默认 NMS，不按距离一刀切删除，也不把固定 detector 坐标的复现当作天空身份；结果需要在注册坐标、WCS/星表和分区注入实验中继续验证。

### 2026-08-29 - 增加匹配后的局部 WCS 校准

星表窗口在先验 WCS 引导的一对一匹配后，允许用户显式点击“根据匹配拟合 WCS”。实现使用切平面东/北角坐标到像素坐标的完整 `2×2` 仿射矩阵，要求至少 6 个有效、非共线匹配点，并用 MAD 规则剔除离群点；UI 显示内点比例、像元角尺度、旋转、parity、RMS 和各向异性。校准结果只在证据充分时进入创新摘要的视场切平面角速度换算，保留“不是盲解算、不是轨道速度”的解释边界。

### 2026-08-29 - 增加遥测末帧常速度位置外推

辅助遥测页和创新报告新增末帧状态向后五个中位帧间隔的常速度位置外推，并输出 `telemetry_prediction.csv`；新增“遥测轨迹”页和 `telemetry_position.png`，将 J2000/WGS84 位置相对首帧投影并标出预测段。界面将结果标记为 `m/s?` 假设下的基线预测，明确不等同于轨道传播或未来位置保证；图像目标的 `px/s` 和遥测载体的 `m/s?` 继续分栏显示。

### 2026-08-29 - 增加单帧与 15 帧分析进度

底部 Tk 状态栏新增确定性进度条。单帧由 FITS 读取、局部背景/PSF 匹配、候选源级测量、质量筛选、逐步长轨迹筛选和预览生成阶段组成；15 帧在每一帧内部显示源级处理比例，完成后显示配准关联、时间中值参考、逐帧残差、线状关联和轨迹拟合。所有后台进度通过主线程队列回填，帧令牌和缓存代际同时参与校验，避免切帧或清理缓存后旧任务覆盖当前状态。

### 2026-08-29 - 增加逐帧线状筛选诊断

线状运动检测新增逐帧审计计数：有效像素、残差支持像素、阈值连通域、面积通过、PCA 几何通过、触边剔除和最终输出线分别记录，15 帧证据窗口增加“线状诊断”页并绘制筛选漏斗。创新导出器同步生成 `motion_frame_audit.csv` 和 `motion_frame_audit.png`，用来解释为什么普通亮点没有进入运动线层；这组计数不冒充逐星真值或误检率。序列缓存版本同步递增，旧缓存会自动失效并重新计算。

### 2026-08-30 - 优化 15 帧性能与进度可见性

**变更内容**：单图继续采用全量候选和逐源精细局部背景；15 帧改为全量 `candidate_count` 审计、每帧前 6000 个 `filter_snr` 候选组成配准工作集、256 px 背景网格和共享局部背景/RMS 图，并默认用 2 个 worker 并行检测不同帧。Tk 进度改为按所有并行帧的完成比例汇总，并合并连续队列事件；状态栏、逐帧表、创新摘要、JSON 和质量图均显示工作集口径。序列缓存版本升至 4，新增 `rst19-ui` 等价入口。

**变更理由**：真实全量精细序列逐帧重复 sigma-clipping，且 UI 每 100 ms 只消费一条事件，导致计算慢、显示明显滞后。性能剖析表明主要成本来自 4096² 背景/滤波，6000 个源级测量只占约 0.57 s；保留 6000 个高 SNR 配准源比继续压低源数更稳妥。

**影响范围**：`detection`、`sequence`、序列缓存、CLI、Tk 进度和创新报告。`returned_count`、`quality_count` 与点轨迹在序列中变为工作集口径；单图总数和最暗可信源仍走全量精细路径。

**决策依据**：真实 15 帧单线程快速路径为 108.165 s，双 worker 为 73.123 s，线状证据和点轨迹汇总在并行前后完全一致；3 worker 为 70.445 s，收益很小而峰值内存更高，因此桌面端默认 2，并通过 CLI 允许 1/3 对照。

### 2026-08-30 - 让序列进度成为主工作区状态

**变更内容**：在检测参数区新增固定的序列进度条和短状态，显示总体百分比、已完成帧数、当前帧号与阶段；底部状态栏继续保留完整任务状态。单图运行时同一位置标注“单图”，避免把单图进度误读成 15 帧进度；切帧、失败、完成、缓存清理均有明确状态。

**变更理由**：底部状态栏在较小窗口中容易被忽略，且此前用户只能看到按钮变为“正在分析”，无法判断 15 帧是否仍在推进。固定进度区不依赖日志长度，也不会被右侧说明文字撑开。

**影响范围**：`gui.py` 的 Tkinter 状态管理和布局；不改变检测数值、缓存键或轨迹判定。

### 2026-08-30 - 继续压缩序列检测等待时间

**变更内容**：序列默认并行 worker 从 2 调整为 3；检测器统计保留单图四轮精测，但序列快速路径的网格背景使用两轮稳健裁剪。`_finite_values` 在已是浮点数组时不再整图升为 `float64`，背景网格插值直接生成所需区域，重复孔径几何数组使用缓存；同时递增单图/序列缓存版本，旧结果不会冒充新口径。主区进度标签改为 `SEQ / 实际帧数`。

**决策依据**：当前 15 张 4096×4096 FITS 在同一台机器上，2 worker 约 67.1 s，3 worker 约 57.1 s，4 worker 约 55.3 s；3 worker 已取得主要吞吐收益，且比 4 worker 更克制峰值内存。2/3/4 worker 的候选数、质量数和线状运动结果一致。

**边界**：两轮网格裁剪只用于 15 帧配准工作集，单图星点总数和最暗源星等继续使用四轮裁剪、环形局部背景和 `float64`；`--workers 1/2/4` 仍可由现场按内存调整。

### 2026-08-30 - 序列像素中间阵列启用精确 float32

**变更内容**：15 帧序列路径在整数 FITS 上启用 `use_float32`，局部背景、匹配滤波和极大值筛选使用 float32 中间阵列；单图路径默认仍用 float64。检测结果记录实际精度，序列缓存版本升至 5。

**变更理由**：真实首帧计时显示 4096² 背景/滤波是主要成本，6000 个源级测量不是瓶颈。当前输入为 16 位整数，float32 能精确表示每个 ADU，实测同一帧候选数和质量数保持一致，降低内存带宽压力。

**影响范围**：`detection.py`、`pipeline.py`、`sequence.py`、缓存键、GUI/CLI 文档和回归测试；单图星点总数与最暗源仍保持原有精细口径。

### 2026-08-30 - 单图长线预检与局部统计等价短路

**变更内容**：单图 GUI 在全量星点精测前增加独立的轻量长线预检，先用稳健背景、有效像素掩膜、残差连通域和 PCA 几何筛选显示橙色 `candidate`；全量星点、SNR、形状和最暗源星等仍在后台继续，预检结果不写入可信星点表，也不升级成 `moving`。同时，`sigma_clipped_stats` 在首轮已保留全部像素时直接复用首轮 center/MAD，仅补算最终标准差，保持原有 `max(1.4826×MAD, std)` 定义，避免每个局部环重复求中位数。

**变更理由**：明显长轨迹不应等到数万候选逐源测光完成后才可见；而单图全量模式不能通过减少候选数换取速度。等价短路减少的是重复统计，不改变单图全量候选、质量筛选和最暗源口径；Tk 通过当前帧令牌丢弃切帧后的预检事件。

**影响范围**：`sequence.py`、`detection.py`、`gui.py`、公共导出、单元测试和 README/运动目标性能记录；15 帧跨帧 `moving` 判定、单图精测和缓存数据边界不变。真实首帧预检约 0.65 s，单图全量一次冷运行约 11.73 s；同口径 15 帧 3 worker 实测约 53.538 s，候选/质量数、线状轨迹和 ID 50885 的仪器星等保持可复核。

### 2026-08-30 - 序列快速路径与实时证据卡

**变更内容**：15 帧序列为每个 256 px 背景块设置 4,096 点的确定性步进抽样；当无效像素比例不超过 2% 时，匹配滤波直接对已置零残差执行一次 Gaussian 卷积，省略掩膜权重的第二次卷积。线性伪迹连通域审计、通量 SNR、点源形状、边缘和饱和规则不删除。`SequenceResult` 记录 `background_sample_limit`、`fast_sequence`，检测结果记录实际 `masked_filter_mode`，缓存版本同步递增。

**界面变化**：任务进度继续由 Tk 主线程消费并合并细粒度事件；除检测参数区和底部状态栏外，右侧 `15-FRAME EVIDENCE` 卡也绑定同一确定性进度变量，显示百分比、`完成 n/15`、当前 `Fxx` 阶段，完成后再替换为时序/配准/速度/预测摘要。这样不会因长文本或右侧布局变化而丢失运行反馈。

**决策依据与边界**：当前真实 15 帧、4 worker、`min_distance=3` 的本机实测约 45.718 s；第 1 帧单帧长线仍是独立 `candidate`，第 9–15 帧仍为 7 点 `moving` 线状候选，位移约 340.774 px、拟合 RMS 约 2.491 px。抽样和单遍卷积可能造成候选峰计数小幅工程级变化，不能与旧缓存或旧实验混列；单图科学测光不启用快速路径，序列 `candidate_count` 仍是全图审计，工作集质量数也不解释为全图恒星真值。

### 2026-08-31 - 增加 3×3 PSF 支持审计并完成真实时序抽检

**变更内容**：在单帧候选的局部测光之后增加独立的 PSF 支持指标 `psf_support_pixels`。它只检查候选峰中心 3×3 原始残差核心，支持阈值为 `max(2×局部噪声, 0.1×峰值超额)`；默认至少 3 个支持像素才进入 `quality_sources`。不通过的源仍保留在 `sources`、CSV 和拒绝标志 `INSUFFICIENT_PSF_SUPPORT` 中，因此不会把审计候选静默删除。

**证据与边界**：真实首帧固定在 `4σ / min_distance=4 / PSF FWHM=3 / flux_snr≥5` 时，候选数仍为 `53,482`，质量源由历史 `25,943` 变为当前 `23,627`；`11,201` 个候选带有 PSF 支持不足标志。对 `20260330163205413_9901.fits` 的 15 帧全量序列复测为：每帧质量 `23,173–24,289`、严格静态 `9,938`、持续候选 `6,249`、稳定星场候选合计 `16,187`，点源 `moving=0`；独立线状层仍为 F09–F15 的 7 帧连续 `moving` 候选，位移 `340.675 px`、拟合 RMS `2.493 px`。这些数值是当前缓存代际 7/11 的可复现工程结果，不是官方逐星真值，也不是向外部 18,000 调参得到的目标数字。

**人工自检**：`tmp/source-quality-audit-psf3/source_cutout_contact_sheet.png` 和 `tmp/sequence-source-audit-psf-support/source_track_contact_sheet.png` 分层显示了源级/时序小图。单像素尖峰在质量层被拒绝；严格静态样本多数在 12–15 帧保持紧凑；持续候选中仍混有低 SNR 暗星与固定热点，所以持续候选必须以低置信度呈现，并继续等待星表、实测 PSF 和人工标签验证。

### 2026-08-31 - 增加通量 SNR 严格层的回收率对照

**变更内容**：在不改变候选阈值、PSF 支持和形状规则的情况下，对 `min_flux_snr=5/6/7/8` 做固定候选集敏感性分析，并补做真实 FITS 背景上的已知 Gaussian 源注入。首帧质量数分别为 `23,627 / 21,016 / 18,808 / 17,138`；当 `SNR≥7` 时，12/16/24/36/56/84/128 ADU 注入档的质量召回为 `0/0/0/0.375/1/1/1`。

**决策边界**：`18,808` 与外部约 `18,000` 接近，但该严格层对弱源有明显损失，不能替代当前默认的 `SNR≥5` 高召回审计层。GUI/CLI 可以提供严格层用于人工复核，正式报告必须同时保留参数、注入真值和召回率；禁止用目标数量反推阈值正确性。原始证据位于 `tmp/real-background-fluxsnr7/real_background_injection.csv`。

**反例记录**：15 帧注册中值后直接检测得到约 `64,573` 个候选和 `41,154` 个质量源，超过单帧 `53,482 / 23,627`；因此中值堆叠只作为弱源发现的研究入口，不作为默认星点数。另一次 7×7 PSF 相关系数小图审计显示，低相关组中仍有清晰紧凑星像，相关系数暂不作为硬拒绝。

### 2026-08-31 - 修复稳定星场完成后不可见

**变更内容**：15 帧分析完成后，主图默认切换到“稳定星场”层；该层按当前选中的 FITS 路径映射到序列结果中的真实 `frame_index`，只绘制该帧实际观测到的 `static/persistent` 点源。稳定点在缩小总览时至少保留一个实心中心，并在状态栏报告当前帧可见数量；“运动候选”仍作为独立图层保留。

**变更理由**：此前序列完成事件无条件把图层切到“运动候选”，稳定星场虽然已计算出结果，却不会立即显示；同时相对路径缓存和 GUI 绝对路径之间只按列表位置取帧，存在把稳定结果请求到错误帧的风险，缩小视图中的单像素空心环也不易辨认。

**影响范围**：`gui.py` 的序列完成状态、帧索引映射、稳定层绘制和悬停查询；新增稳定层与相对缓存路径回归测试。该修复不改变检测、轨迹分类、缓存数据或星点统计口径。

**验证依据**：真实序列结果中当前帧稳定层可见点数为 `10,975–16,187`；无窗口渲染检查在 F01 生成 `11,134` 个稳定层点的青绿色/蓝色像素，定向 GUI 测试全部通过。

### 2026-08-30 - 共享背景模型与逐帧状态带

**变更内容**：序列快速路径先由首帧建立二维 background/RMS 模型，后续同尺寸帧复用；每帧仍独立计算全局统计、坏点/饱和掩膜、PSF 匹配、线性伪迹审计和源级质量规则，尺寸不一致时回退到逐帧模型。`SequenceResult.background_model_mode` 和创新报告的 `quality.background_model_mode` 记录实际口径，序列缓存版本升至 9。Tk 参数区增加 F01…F15 帧状态带，并把“建立共享背景模型”作为显式进度阶段。

**决策依据与边界**：同一真实数据、4 worker、工作集 6000 的复测约 43.767 s/15 帧（运行时间随系统负载略有波动），相对共享模型前约 47.572 s 再缩短约 8%；F01 单帧 `candidate`、F09–F15 连续 `moving` 线状证据保持不变。共享背景可能让全量候选峰计数相对逐帧局部模型出现工程级变化，因此不与旧缓存混报；单图科学测光不使用该共享模型。

### 2026-08-30 - 弱源可见性与人工阈值反馈

**变更内容**：预览显示与检测数值彻底分离：GUI 保留当前 4096² 传感器空间分辨率，显示层用 `1.0/99.5` 分位、稳健 IQR 噪声底、gamma `1.15` 和 `0.6 px` 高斯平滑生成观察图，检测仍使用 FITS 原始 ADU；总览时质量/候选覆盖使用单像素证据点，放大后才扩成屏幕空间小方框，避免数万候选把背景盖满。主参数区新增候选阈值和可信通量 SNR 滑块，拖动不连续启动重算，点击“应用并分析”才运行当前帧；已完成的运行可按“保留弱星/减少伪点/当前平衡”追加到 `tmp/manual-threshold-feedback.jsonl`。

**变更理由与边界**：真实首帧的 `ID 9296` 已在候选表中，`filter SNR=4.57`、`flux SNR=3.07`，原 `flux SNR≥5` 只会把它放入拒绝候选而不是可信层；在 `flux SNR≥3` 的对照运行中它通过质量条件。这个反馈文件是后续离线调参的证据，不是自动在线学习，也不把降低阈值后的数量直接称为恒星真值。

**影响范围**：`gui.py` 的预览、覆盖标记和参数控件，新增 `feedback.py` 的 JSONL 读写，以及 GUI/反馈单元测试；同时调整 viewer 标题与图层选择的 Tk pack 顺序，避免矮窗口把主图压缩到不可见；不改变单图检测器的原始测光公式、缓存键或 FITS 文件。

### 2026-08-31 - 原始/噪声观察层与悬停质量证据

**变更内容**：主图新增“增强显示 · 轻度降噪”“原始显示 · 未降噪”“增亮噪声 · 看弱点”三个观察层。三层共用一次稳健统计并按帧缓存，切换时只更换预览图，不改变 FITS 原始 ADU、检测阈值、SNR、测光或星等。源悬停详情补充 peak/flux/filter 三类 SNR、通量误差、形态、PSF 支持、质心偏移和质量标记翻译；图内短标签改用 ASCII，并增加 Windows 中文字体回退，修复中文/Unicode 字符显示为方框的问题。

**决策依据与边界**：原始层用于核对真实像素，增亮噪声层用于观察弱点与背景起伏，不能单独作为恒星真值。悬停的“落选原因”来自检测器已经写入的 flags 和当前参数，`MASKED`、`LOW SNR` 等标签是质量筛选证据，不代表天体分类结论；“全部候选”层才展示包括落选源在内的完整候选集。

**影响范围**：`gui.py` 的预览构造、Tk 显示控件、源标签和悬停信息，以及 GUI 证据测试；未修改 FITS 原始文件和检测/缓存数据口径。

### 2026-09-01 - 第三提案 Starlet 与三模式同坐标实验

**变更内容**：在 Gaussian 基线和 Gaussian+双尺度 DoG hybrid 之外，增加 B3-spline à trous 的 `w1=c0-c1`、`w2=c1-c2` 两层 Starlet 提案。所有算法先合并候选并记录 `proposal_methods/proposal_scales/proposal_snr`，再统一回到原始 FITS 执行局部测光、通量 SNR、3×3 PSF 支持、形态、掩膜、线状和单/双 PSF BIC 细筛。GUI 将其作为“小波实验 · +Starlet”独立模式，不改变 GUI 当前的 `hybrid` 默认。

**实验决策**：首帧快速提案耗时/候选为 Gaussian `3.79 s/53,532`、hybrid `7.68 s/61,656`、ensemble `11.06 s/66,108`；完整 ensemble 为 `66,048` 候选、`24,275` 质量源，相对 hybrid 增加 `4,463/144`。同坐标孤立源、FWHM 失配和拥挤双源实验均未观察到 Starlet 相对 hybrid 的召回提升；反相阴性图三模式最终都只剩 2 个质量源。因此 Starlet 只作为可审计研究入口，不能因候选更多或“小波”名称而宣称更准确。

**影响范围**：`detection.py`、pipeline/cache/CLI、三模式注入比较器、GUI 模式和悬停来源字段、论文实验记录及回归测试。缓存键包含 Starlet 阈值；旧结果不会伪装成新模式结果。

### 2026-09-01 - 修复异步预览完成后的界面假卡死

**问题与修复**：预览线程实际约 1.2 秒即可生成三张 4096² 显示图，但成功事件只写入画布和悬浮提示，没有清除底部“正在载入预览”状态，也没有刷新按钮状态。新增主线程 `_apply_preview_result`，统一提交图像、重置视图、更新状态/提示、刷新操作控件后再绘制。

**验证**：新增预览完成状态回归测试；真实 Tk 窗口已验证预览载入、单帧确定性进度、缓存恢复、切换第二帧、可信星点图层和 15× 缩放。15 帧任务同时显示总百分比、`完成 n/15`、当前 Fxx 阶段和并行帧状态格。

### 2026-09-01 - 时序补提案局部峰重定位与宽筛细筛证据

**变更内容**：时间中值/稳健叠加提出的坐标不再直接在四舍五入后的单个整数像素上做逐帧强制响应；当前在预测点 `±1 px` 的 `3×3` 窄窗口内选择最高匹配响应，再用该局部峰执行中心有效性、`support_3x3`、形态和 PSF 相关细筛。序列缓存代际升至 29。Gaussian、DoG 和 Starlet 仍按候选并集宽筛，全部回到原始 FITS 细筛；GUI 当前默认使用 `hybrid + FWHM=2 px`，ensemble 仅作为召回审计模式。

**真实抽测**：同一 15 帧、`reference SNR=15`、PSF 相关 `0.80` 的 Gaussian 快速口径从固定整数点的 `5,276 / 14,919 / 20,195`（严格静态/持续/合计）变为 `5,276 / 15,524 / 20,800`，PSF 相关拒绝由 `1,336` 降至 `669`。同一新代码的 ensemble 为 `4,214 / 21,682 / 25,896`，相对 Gaussian 多出约 `6,282` 条低置信坐标；它不被解释为新增恒星，因为其中仍有单帧质量拒绝和结构性伪影可能。

**边界**：`±1 px` 是注册/取整的定位修正，不是降低 SNR 或扩大远邻搜索；多算法并集是 `N_wide`，不能直接作为星数。当前仍缺设备级暗场/坏点表、官方逐星真值、分区实测 PSF 和经过阴性样本校准的源级 FDR；正式数量需要星表、人工注释和分层注入回收共同冻结。

### 2026-09-02 - GUI 单帧/15 帧默认分析口径切换

**变更内容**：单帧和 15 帧 GUI 入口统一使用 `proposal_mode=hybrid`、`PSF FWHM=2 px`、候选阈值 `4σ`、`min_distance=4 px`、通量 `SNR≥5`。15 帧默认使用 `median` 时序提案、参考 SNR `15`、普通候选共识 `15`、逐帧补测 `7.5`、PSF 相关 `≥0.80` 和预测点 `±1 px` 局部峰重定位；时序多尺度、局部去混叠和 15 帧全量关联默认关闭。

**决策依据与边界**：旧版经验 PSF 的中位 FWHM 约 `1.934 px` 来自模板邻域筛选修复前的历史实验，不能当作当前模板统计。修复后首帧从完整候选表中筛出 20 个干净隔离质量源，经验核中位测得 FWHM 约 `2.497 px`；同一截图局部的经验核双源证据为 `ΔBIC=-3.13`、次分量 SNR `1.36`，未达到双源确认线。hybrid 仍是当前偏召回的平衡默认，不是“全面更准”。当前首帧按最新数据有效性规则复算为 `84,594` 个候选、`29,153` 个质量源；`29,271` 是只加入极端负码审计的中间版本。结果继续分开记录候选、质量、严格静态、持续候选和拒绝 flags。`local_deblend` 的分钟级控制收益不足以默认开启。完整实测表见 `doc/02-星图识别/检测器实验记录.md` 的 8.11–8.12 节及 8.38。

### 2026-09-02 - 增加真实背景条件分层注入审计

新增 `run_stratified_real_background_injection` 和 `rst19-injection --stratified-real`，在真实首帧的低纹理/高局部噪声、边缘、拥挤和特殊值域邻域分别注入已知经验 PSF，并同时输出候选层、质量层和严格新增源召回。真实 FITS 的负溢出边界由基线检测显式传给浮点注入副本，避免 `int16 → float32` 后实验改变原始异常值审计口径。

该设计把 `ambiguous_injection_count`（8 px 邻域内存在基线候选）与 `unambiguous_injected_count`（排除 3 px 直接匹配邻源后的分母）分开；不把拥挤/特殊值域附近的原有源重复算成注入成功。首轮每层每档只有 2 个位置，只用于证明条件机制：低纹理/高噪声在 128 ADU 的质量召回为 `0.5`，边缘质量召回为 `0`，拥挤层最高档为 `1`，特殊值域严格新增源三档均为 `0` 且 `2/2` 有 8 px 邻源歧义；单独的 `line` 邻域控制在严格分母 `2/2` 下三档也均为 `0`，局部 RMS 约 `558.20 ADU`。该结果支持“宽筛候选—条件细筛—身份确认”的论文口径，但不被解释成真实星表完备率；完整坐标和局部统计写入 `stratified_real_background_injection.csv/json/png`，测试覆盖见 `tests/test_experiments.py`。

### 2026-09-02 - 扩大真实背景分层样本并固定论文主表口径

在不改变检测器默认参数的前提下，将真实背景条件注入从每格 2 个位置扩大到每格 8 个位置，普通层与 `line` 层分别输出。论文主表使用严格新增源召回；8 px 邻源只作为歧义字段，`special_code` 因 8/8 位置均无独立新源分母而不解释为孤立完备率。扩大样本显示低纹理、高局部 RMS、边缘截断、拥挤混合和线状残差具有不同回收瓶颈，因此默认流程保持“宽筛候选—条件细筛—身份确认”，不因某一层的召回数字自动调整全局阈值。逐点条件写入实验 CSV/JSON/PNG，短论文只保留条件表与机制结论。

### 2026-09-02 - 截图双框的 NMS 间距消融

在同一辅助掩膜下将 `min_distance` 从 `4/5` 调到 `6/8 px`，截图副峰才被非极大值抑制合并；全图候选/质量从 `84,594/29,271` 变化到 `61,324/24,992`。该结果确认 `min_distance` 是候选账本参数而非物理分辨率，因此继续保留 `4 px` 宽筛默认，近邻真实性交给 PSF、值域和跨帧证据。

同一真实背景的等强双源控制补充显示，`3 px` 间距在 56/128 ADU 下召回均为 `0.5/0.5`，`5/6 px` 在 128 ADU 下为 `1.0/1.0`，且三种提案模式一致。该结果只约束 PSF 重叠下的几何可分性，不替代截图副峰的异常值域与跨帧核验。

### 2026-09-03 - 固定总峰值超额的双源强弱比与经验 PSF 对照

新增 `run_pair_flux_ratio_audit`，在同一真实背景、同一双源几何和同一组位置上固定 `A_p+A_s`，扫描 `A_s/A_p`，并以两个不同候选一对一命中定义 `pair_resolution_recall`。扩展到总峰值 `256/512/1024 ADU` 后，Gaussian `r=0.125` 的成对质量解析率为 `0/0.25/1.0`；`256 ADU、r=0.25` 仍有 `1.0` 成对候选解析而质量解析为 `0`。这把弱副源先失去细筛证据、再失去宽筛响应的转折从单一截图现象提升为可控条件对照。

新增 `signal_normalization=integrated_excess`，用单位峰值离散核的和把每个注入源归一化到相同积分超额，避免 Gaussian 与经验 PSF 的核和差异混入公平比较。总积分超额 `1867 ADU`、同一布局、`2×8` 对/条件下，`r=0.25/0.125` 的成对质量解析率为 Gaussian `0.812/0.188`、empirical `0.625/0.125`；候选层在 `r=0.125` 为 `0.688/0.812`。结果只能支持 PSF 形状影响细筛且候选/质量层可能反向排序，不能给出最终 PSF 排名；仍需分区模板、更多 trial 和完整噪声标定。

另用首帧 20 个干净质量源构造经验核，在总峰值 `512 ADU`、`r=0.25/0.125` 下得到成对候选/质量解析率 `1.000/0.250` 与 `0.750/0.000`。该组仍是 PSF 形状/尺度敏感性控制：检测器 FWHM 仍为 `2 px`，经验核中位 FWHM 约 `2.497 px`，不能直接排名 Gaussian/empirical。实验只用于支持“候选峰、双源解析、质量确认三层分离”，不估计真实双星比例；输出目录和限制见 `doc/02-星图识别/检测器实验记录.md` 8.32。

### 2026-09-03 - 增加真实背景 2/3 源拥挤组审计

新增 `run_crowded_blend_audit` 和 `rst19-injection --crowded-blend-audit`。它在真实首帧上沿随机方向注入等间距的 2 源/3 源组，固定组总离散积分信号，并分别输出单源召回、组级一对一完整解析率、合并率和组邻域额外候选率。`group_resolution` 要求组内每个真值由不同候选命中；`merged` 表示一个候选同时接近至少两个真值；`extra` 只表示保护区内候选数超过注入真值数，三者都不是 precision。

扩展到每条件 `2 trials × 8` 组后，`3 px` 的组级候选解析率仍为 `0`、合并率为 `0.750--1.000`；`5.4/6 px` 的候选组级解析率仍为 `1.0` 且未观察到组内额外候选。总积分由 `512` 提到 `1024 ADU` 后，2 源 `5.4/6 px` 的质量级完整解析为 `15/16`、`14/16`，但 3 源仅 `1/16`、`0/16`。这把 PSF 几何合并和每源信号不足造成的质量层损失分开，但仍不是正式完备率；经验 PSF 敏感性控制已经补做，后续需要更多布局/trial、空间 PSF 和多源联合拟合。

经验 PSF 的 `4 组/条件` 敏感性控制显示，`3 px` 仍不能组级解析，`5.4 px` 候选层仍可分开，但 `6 px` 的候选组解析可下降到 `0.75`（2 源）或 `0.25`（3 源）。因此间距阈值不能脱离 PSF 形状、方向、每源信号和局部背景；该结果仅作为控制，不能排名两种 PSF。

### 2026-09-03 - 增加截图局部固定位置多源 PSF 审计

新增 `LocalMultiPSFAuditResult`、`run_local_multipsf_audit` 和 `rst19-multipsf-audit`（也可用 `python -m rst19.multipsf_cli`），对用户截图对应的三个候选位置在同一局部窗口内比较 `K=1/2/3` 个固定中心 Gaussian PSF。模型共享二维背景平面，源幅度使用 NNLS 的非负约束，并输出 RSS、BIC、`ΔBIC`、分量 SNR、模板相关和设计矩阵条件数；同时支持 `raw`、范围码屏蔽和精确 `-1` 屏蔽三种审计口径。

该工具故意不接入 GUI 默认检测或修改星数。它的职责是把“检测器找到两个局部峰”和“多个独立 PSF 得到模型支持”分开：真实 15 帧 raw 结果只有 F07/F12 的 `K=2` BIC 优于单源，但新增分量 SNR 约 `4.38/4.00`，没有帧同时越过 `ΔBIC≥10` 与 `SNR≥5`；`K=3` 没有确认。范围码屏蔽后 F07 形式上越线，但该帧仍为 `MASKED|SATURATED`，因此仅记作值域/掩膜敏感性。输出保存到 `local_multipsf_audit.csv/json` 和 BIC 图。

**设计边界**：固定中心、全局 Gaussian、共享背景和最小平方拟合不是完整天文测光似然；`ΔBIC` 不是恒星后验概率，分量 SNR 也不是跨帧身份。后续必须以分区留出 PSF、自由位置多源联合拟合、设备级坏像素/满阱定义、注入回收和官方星表/WCS 继续筛查，才能升级为物理源结论。

### 2026-09-03 - 增加有界自由位置与半径敏感性审计

新增 `run_local_free_multipsf_audit`、`run_local_free_multipsf_radius_sweep` 和 `rst19-free-multipsf-audit`。它在固定候选初值附近以有限半径优化 PSF 中心，保留非负幅度、共享背景和相同 `K=1/2/3` 模型，并将位置触边、优化器收敛状态、拟合中心偏移和分量 SNR写入结果。`--radius-sweep` 可扫描多个半径，不把任一单半径的通过结果当作稳定结论。

真实 raw 半径扫描 `0.25/0.5/0.75/1.0/1.25 px` 的 `K=2` 确认数为 `0/15、0/15、0/15、1/15、1/15`；固定中心为 `0/15`。F07 的新增位置在宽半径下移动约 `1.01 px`，窄半径时触碰边界。该差异是“固定坐标误差”和“自由度吸收局部结构”之间的敏感性证据，不能升级为双星比例；研究工具不接入 GUI 默认计数。

### 2026-09-03 - 第二组截图双框的数据有效性门控

复核首帧 `(2439,4021)` 与 `(2435,4022)` 后确认：整数峰间距为 `4.12 px`，略大于 GUI 的 `min_distance=4 px`，因此 NMS 会保留两个局部响应；测量质心间距约 `2.74 px`，属于当前 `FWHM=2 px` Gaussian 的近邻重叠区。门控前两源的峰值/通量 SNR 约为 `3992/204.61` 与 `569/115.51`，普通质量规则不足以识别其共享异常值域。

新增 `Detection.repeated_code_count`、`range_anomaly_pixel_count`、`repeated_code_values` 和 `CODE_PATTERN` 旗标：全幅重复高位码只作为统计先验，必须和候选孔径的异常负值共同出现才进入质量门；候选不删除，质量层降级，GUI 悬浮窗显示码值和异常像素数。F01 候选仍为 `84,594`，质量层由门控前 `29,295` 变为 `29,153`。该门控不是相机 `BLANK`、ADC 饱和或坏像素标定，待主办方设备资料和星表/WCS 验证后再调整。

### 2026-09-03 - 修正 CODE_PATTERN 对真实亮星的误拒

初版 `CODE_PATTERN` 用“孔径内含重复高位码 + 孔径含异常负值”作为充分条件，但真实首帧的亮星在饱和/溢出出血列里会同时带入 `3991/3992` 等重复码和 `-20628` 这类负值，峰值却仍是正常高亮度值（例如 `ID 24651`：峰值 `20893`、`flux_snr≈775.9`、`FWHM≈2.84`、椭圆率 `0.044`），却因此被误标为数据编码伪迹并移出质量层。

修正后只有**候选峰本身落在全幅异常重复高位码上**、且孔径含异常负值，才判定 `CODE_PATTERN`；仅“孔径内含重复码”不再构成拒绝理由。真实首帧同参复算：`CODE_PATTERN` 由 `159` 降到 `12`（这 `12` 个峰值都恰为 `3990–3993` 且紧邻更强源），质量源由 `29,153` 升到 `29,263`；`ID 24651` 现在通过质量层。该修正只影响数据有效性门，不改变宽筛候选、通量、星等或 PSF/形状规则。

### 2026-09-03 - 增加叠加暗星恢复层

在 `sequence.py` 增加 `_stack_faint_tracks`：对注册后的 15 帧时间中值（或稳健均值）参考图，用参考图自身降噪后的噪声底以 `4σ`、`flux_snr≥5` 重新检测，再回到每帧原始图做 `±1 px` 局部峰强制测光、`3×3` 支持、形状和边缘/掩膜审计，要求达到 `persistent` 出现帧数且逐帧放宽 `flux_snr≥3`。结果标为 `evidence_level="stack_faint"`、`classification="persistent"`，单独计为 `stack_faint_count`，不与 `persistent_source_count`、`stable_source_count` 或单帧 `quality_count` 混写；GUI 新增“叠加暗星 · 待复核”图层，`rst19-sequence` 默认开启、可用 `--no-stack-faint` 关闭。这修正了 8.41 的旧结论：那些亮星邻域的“肉眼可见亮点”不是噪声，而是单帧噪声底压住的真实暗星，只能通过降噪叠加 + 逐帧强制测光恢复，不能靠无差别调低单帧阈值。方法依据见 [最优图像叠加检测](http://arxiv.org/pdf/astro-ph/9310031) 和 [proper image subtraction](https://ar5iv.labs.arxiv.org/html/1601.02655)。

逐帧强制测光已向量化（`_stack_forced_frame_measure_batch`，按 `chunk_size` 分块）并按帧用 `ThreadPoolExecutor` 并发；本机 15 帧全流程由约 `450 s` 降到约 `310 s`，`stack_faint` 计数 `9,857→9,814`（`−0.4%`）。

新增规则专门保护“宽筛召回、质量确认、物理身份”三层边界；它不按峰数或外部 `18,000` 示例裁剪，也不把本组的两个候选直接宣称为两颗星。固定/自由多源结果及局部像素证据见 `doc/02-星图识别/检测器实验记录.md` 8.38 和 `doc/02-星图识别/亮点真实性与伪影判别研究.md` 10.25。
